<UserControl x:Class="LaserConfigurator.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1600">
    <Grid>
        <!-- Градиентный фон для всего приложения -->
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F0F4F8" Offset="0"/>
                <GradientStop Color="#E1E8ED" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления конфигурацией -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#30000000" BlurRadius="20" ShadowDepth="4" Opacity="0.3"/>
            </Border.Effect>
            <StackPanel>
                <!-- Градиентный заголовок -->
                <Border CornerRadius="12,12,0,0" Padding="20,15">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#667EEA" Offset="0"/>
                            <GradientStop Color="#764BA2" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M12,8A4,4 0 0,1 16,12A4,4 0 0,1 12,16A4,4 0 0,1 8,12A4,4 0 0,1 12,8M12,10A2,2 0 0,0 10,12A2,2 0 0,0 12,14A2,2 0 0,0 14,12A2,2 0 0,0 12,10M10,22C9.75,22 9.54,21.82 9.5,21.58L9.13,18.93C8.5,18.68 7.96,18.34 7.44,17.94L4.95,18.95C4.73,19.03 4.46,18.95 4.34,18.73L2.34,15.27C2.21,15.05 2.27,14.78 2.46,14.63L4.57,12.97L4.5,12L4.57,11L2.46,9.37C2.27,9.22 2.21,8.95 2.34,8.73L4.34,5.27C4.46,5.05 4.73,4.96 4.95,5.05L7.44,6.05C7.96,5.66 8.5,5.32 9.13,5.07L9.5,2.42C9.54,2.18 9.75,2 10,2H14C14.25,2 14.46,2.18 14.5,2.42L14.87,5.07C15.5,5.32 16.04,5.66 16.56,6.05L19.05,5.05C19.27,4.96 19.54,5.05 19.66,5.27L21.66,8.73C21.79,8.95 21.73,9.22 21.54,9.37L19.43,11L19.5,12L19.43,13L21.54,14.63C21.73,14.78 21.79,15.05 21.66,15.27L19.66,18.73C19.54,18.95 19.27,19.04 19.05,18.95L16.56,17.95C16.04,18.34 15.5,18.68 14.87,18.93L14.5,21.58C14.46,21.82 14.25,22 14,22H10M11.25,4L10.88,6.61C9.68,6.86 8.62,7.5 7.85,8.39L5.44,7.35L4.69,8.65L6.8,10.2C6.4,11.37 6.4,12.64 6.8,13.8L4.68,15.36L5.43,16.66L7.86,15.62C8.63,16.5 9.68,17.14 10.87,17.38L11.24,20H12.76L13.13,17.39C14.32,17.14 15.37,16.5 16.14,15.62L18.57,16.66L19.32,15.36L17.2,13.81C17.6,12.64 17.6,11.37 17.2,10.2L19.31,8.65L18.56,7.35L16.15,8.39C15.38,7.5 14.32,6.86 13.12,6.62L12.75,4H11.25Z"
                              Fill="White" Width="24" Height="24" Stretch="Uniform" Margin="0,0,12,0"/>
                        <TextBlock Text="🔧 Управление конфигурацией" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Кнопки управления -->
                <WrapPanel Margin="20,15,20,20">
                        <Button Content="Загрузить конфиг"
                                Command="{Binding LoadConfigCommand}"
                                Style="{StaticResource ButtonPrimary}"
                                Margin="0,0,10,0"/>
                        <Button Content="Сохранить конфиг"
                                Command="{Binding SaveConfigCommand}"
                                Style="{StaticResource ButtonSuccess}"
                                Margin="0,0,10,0"/>
                        <Button Content="Применить изменения"
                                Command="{Binding ApplyConfigCommand}"
                                Style="{StaticResource ButtonInfo}"
                                Margin="0,0,10,0"/>
                        <Border Width="2" Background="{DynamicResource BorderBrush}" Margin="10,5"/>
                        <Button Content="Вручную подключить сканатор 1 (227) "
                                Command="{Binding ConnectScanner1Command}"
                                Style="{StaticResource ButtonWarning}"
                                Margin="0,0,10,0"/>
                        <Button Content="Вручную подключить сканатор 1 (228)"
                                Command="{Binding ConnectScanner2Command}"
                                Style="{StaticResource ButtonWarning}"
                                Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding ConnectionStatusText}"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"
                                   FontWeight="Bold"
                                   Foreground="#667EEA"
                                   FontSize="14"/>
                    </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Левая панель: Параметры фигуры -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,8,0">
                <Border.Effect>
                    <DropShadowEffect Color="#20000000" BlurRadius="15" ShadowDepth="3" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок секции -->
                    <Border Grid.Row="0" CornerRadius="10,10,0,0" Padding="15,12">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#F59E0B" Offset="0"/>
                                <GradientStop Color="#F97316" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="📐 Параметры фигуры" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel>

                        <!-- Тип фигуры -->
                        <TextBlock Text="Тип фигуры:" Margin="0,0,0,5"/>
                        <ComboBox SelectedIndex="{Binding SelectedShapeTypeIndex}" Margin="0,0,0,15">
                            <ComboBoxItem Content="Квадрат"/>
                            <ComboBoxItem Content="Круг"/>
                            <ComboBoxItem Content="Треугольник"/>
                            <ComboBoxItem Content="Крест"/>
                        </ComboBox>

                        <!-- Координаты -->
                        <TextBlock Text="X (мм):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding ShapeX}"
                                          Increment="1"
                                          DecimalPlaces="2"
                                          Margin="0,0,0,10"/>

                        <TextBlock Text="Y (мм):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding ShapeY}"
                                          Increment="1"
                                          DecimalPlaces="2"
                                          Margin="0,0,0,10"/>

                        <!-- Размер -->
                        <TextBlock Text="Размер (мм):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding ShapeSize}"
                                          Increment="1"
                                          DecimalPlaces="2"
                                          Margin="0,0,0,10"/>

                        <!-- Параметры лазера -->
                        <Separator Margin="0,10,0,15"/>
                        <TextBlock Text="Лазер" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock Text="Скорость (мм/с):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding MarkSpeed}"
                                          Minimum="100"
                                          Maximum="5000"
                                          Increment="100"
                                          DecimalPlaces="0"
                                          Margin="0,0,0,10"/>

                        <TextBlock Text="Мощность (Вт):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding LaserPower}"
                                          Minimum="10"
                                          Maximum="500"
                                          Increment="10"
                                          DecimalPlaces="0"
                                          Margin="0,0,0,10"/>

                        <TextBlock Text="Диаметр (мкм):" Margin="0,0,0,5"/>
                        <hc:NumericUpDown Value="{Binding BeamDiameter}"
                                          Minimum="40"
                                          Maximum="200"
                                          Increment="5"
                                          DecimalPlaces="0"
                                          Margin="0,0,0,10"/>

                        <Separator Margin="0,10,0,15"/>
                        <CheckBox Content="Разделить между лазерами"
                                  IsChecked="{Binding SplitBetweenLasers}"
                                  Margin="0,0,0,10"/>

                        <!-- Выбор сканатора для одиночного режима -->
                        <TextBlock Text="Сканатор (одиночный режим):" Margin="0,0,0,5"/>
                        <ComboBox SelectedIndex="{Binding SelectedScannerIndex}"
                                  Margin="0,0,0,15">
                            <ComboBoxItem Content="Сканатор 1 (227)"/>
                            <ComboBoxItem Content="Сканатор 2 (228)"/>
                        </ComboBox>

                        <Button Content="Нарисовать" HorizontalAlignment="Stretch"
                                Command="{Binding DrawShapeCommand}"
                                Style="{StaticResource ButtonPrimary}"
                                Height="40"
                                Margin="5,0,5 0"/>

                        <Button Content="Очистить" HorizontalAlignment="Stretch" Margin="5 5 5 0"
                                Command="{Binding ClearPreviewCommand}"
                                Style="{StaticResource ButtonWarning}"
                                Height="40"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Центральная панель 1: Конфигурация Сканатора 1 -->
            <Border Grid.Column="2" Background="White" CornerRadius="10" Margin="8,0">
                <Border.Effect>
                    <DropShadowEffect Color="#20000000" BlurRadius="15" ShadowDepth="3" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок секции -->
                    <Border Grid.Row="0" CornerRadius="10,10,0,0" Padding="15,12">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#10B981" Offset="0"/>
                                <GradientStop Color="#059669" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="🔷 Конфигурация Сканатора 1" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel DataContext="{Binding Scanner1Config}">

                        <!-- CardInfo -->
                        <TextBlock Text="Информация о карте" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding CardInfo}">
                            <StackPanel>
                                <TextBlock Text="IP адрес:" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding IpAddress}" Margin="0,0,0,8"/>

                                <TextBlock Text="Индекс:" Margin="0,0,0,3"/>
                                <hc:NumericUpDown Value="{Binding SeqIndex}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                <TextBlock Text="Комментарий:" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding Comment}" Margin="0,0,0,8"/>
                            </StackPanel>
                        </Border>

                        <!-- ScannerConfig -->
                        <TextBlock Text="Конфигурация сканера" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ScannerConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="FieldSizeX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FieldSizeX}" Minimum="0" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="FieldSizeY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FieldSizeY}" Minimum="0" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="ProtocolCode:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ProtocolCode}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                    <TextBlock Text="CoordinateTypeCode:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CoordinateTypeCode}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                    <TextBlock Text="OffsetX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetX}" Minimum="-1000" Maximum="1000" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="OffsetY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetY}" Minimum="-1000" Maximum="1000" DecimalPlaces="3" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="OffsetZ:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetZ}" Minimum="-1000" Maximum="1000" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="ScaleX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleX}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="ScaleY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleY}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="ScaleZ:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleZ}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="RotateAngle:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding RotateAngle}" Minimum="-360" Maximum="360" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- BeamConfig -->
                        <TextBlock Text="Конфигурация пучка" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding BeamConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="MinBeamDiameterMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MinBeamDiameterMicron}" Minimum="0" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="WavelengthNano:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding WavelengthNano}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="RayleighLengthMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding RayleighLengthMicron}" Minimum="0" Maximum="100000" DecimalPlaces="1" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="M2:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding M2}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="FocalLengthMm:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FocalLengthMm}" Minimum="0" Maximum="10000" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- LaserPowerConfig -->
                        <TextBlock Text="Конфигурация мощности лазера" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding LaserPowerConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="MaxPower:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MaxPower}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="PowerOffsetKFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding PowerOffsetKFactor}" Minimum="-100" Maximum="100" DecimalPlaces="6" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="PowerOffsetCFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding PowerOffsetCFactor}" Minimum="-1000" Maximum="1000" DecimalPlaces="6" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- ThirdAxisConfig -->
                        <TextBlock Text="Конфигурация третьей оси" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ThirdAxisConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="AFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Afactor}" Minimum="-100" Maximum="100" DecimalPlaces="6" Margin="0,0,0,8"/>

                                    <TextBlock Text="BFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Bfactor}" Minimum="-100" Maximum="100" DecimalPlaces="10" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="CFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Cfactor}" Minimum="-1000" Maximum="1000" DecimalPlaces="6" Margin="0,0,0,8"/>

                                    <TextBlock Text="BaseFocal:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding BaseFocal}" Minimum="0" Maximum="10000" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- FunctionSwitcherConfig -->
                        <TextBlock Text="Переключатели функций" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15">
                            <Grid DataContext="{Binding FunctionSwitcherConfig}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Коррекция мощности (Offset)" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="0" Grid.Column="1" IsChecked="{Binding EnablePowerOffset}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Коррекция мощности (Correction)" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="1" Grid.Column="1" IsChecked="{Binding EnablePowerCorrection}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Z коррекция" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="2" Grid.Column="1" IsChecked="{Binding EnableZCorrection}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Изменение диаметра пучка" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="3" Grid.Column="1" IsChecked="{Binding EnableDiameterChange}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Динамическая смена переменных" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="4" Grid.Column="1" IsChecked="{Binding EnableDynamicChangeVariables}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Ограничение Min точки" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="5" Grid.Column="1" IsChecked="{Binding LimitVariablesMinPoint}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ограничение Max точки" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="6" Grid.Column="1" IsChecked="{Binding LimitVariablesMaxPoint}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Переменная задержка прыжка" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="7" Grid.Column="1" IsChecked="{Binding EnableVariableJumpDelay}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>
                            </Grid>
                        </Border>

                        <!-- NonDepends ProcessVariables -->
                        <TextBlock Text="Базовые параметры процесса (NonDepends)" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ProcessVariablesMap.NonDepends[0]}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="MarkSpeed:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MarkSpeed}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpSpeed:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpSpeed}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="PolygonDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding PolygonDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="MarkDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MarkDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOnDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOnDelay}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOffDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOffDelay}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="LaserOnDelay (SkyWriting):" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOnDelayForSkyWriting}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOffDelay (SkyWriting):" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOffDelayForSkyWriting}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="CurBeamDiameterMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CurBeamDiameterMicron}" Minimum="0" Maximum="1000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="CurPower:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CurPower}" Minimum="0" Maximum="1000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpMaxLengthLimitMm:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpMaxLengthLimitMm}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="MinJumpDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MinJumpDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="Umax:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Umax}" Minimum="0" Maximum="10" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <CheckBox Content="SkyWriting Enable" IsChecked="{Binding Swenable}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- MarkSpeed Profiles -->
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Профили MarkSpeed" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Добавить профиль"
                                    Command="{Binding DataContext.AddMarkSpeedProfile1Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Style="{StaticResource ButtonSuccess}"
                                    Margin="5,0,0,0"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding DataContext.Scanner1MarkSpeedProfiles, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding MarkSpeed, StringFormat='Скорость: {0} мм/с'}"
                                                           FontWeight="Bold" VerticalAlignment="Center"/>
                                                <Button Grid.Column="1" Content="Редактировать"
                                                        Command="{Binding DataContext.EditMarkSpeedProfile1Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource ButtonInfo}"
                                                        Margin="5,0"/>
                                                <Button Grid.Column="2" Content="Удалить"
                                                        Command="{Binding DataContext.DeleteMarkSpeedProfile1Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource ButtonDanger}"/>
                                            </Grid>

                                            <Expander Grid.Row="1" Header="Детали" Margin="0,10,0,0">
                                                <Grid Margin="10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                                        <TextBlock Text="{Binding JumpSpeed, StringFormat='JumpSpeed: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding PolygonDelay, StringFormat='PolygonDelay: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding JumpDelay, StringFormat='JumpDelay: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding MarkDelay, StringFormat='MarkDelay: {0}'}" Margin="0,2"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                                        <TextBlock Text="{Binding LaserOnDelay, StringFormat='LaserOnDelay: {0:F1}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding LaserOffDelay, StringFormat='LaserOffDelay: {0:F1}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding CurBeamDiameterMicron, StringFormat='BeamDiameter: {0:F1} μm'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding CurPower, StringFormat='Power: {0:F1} W'}" Margin="0,2"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Expander>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Центральная панель 2: Конфигурация Сканатора 2 -->
            <Border Grid.Column="4" Background="White" CornerRadius="10" Margin="8,0">
                <Border.Effect>
                    <DropShadowEffect Color="#20000000" BlurRadius="15" ShadowDepth="3" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок секции -->
                    <Border Grid.Row="0" CornerRadius="10,10,0,0" Padding="15,12">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#3B82F6" Offset="0"/>
                                <GradientStop Color="#2563EB" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="🔶 Конфигурация Сканатора 2" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15">
                        <StackPanel DataContext="{Binding Scanner2Config}">

                        <!-- CardInfo -->
                        <TextBlock Text="Информация о карте" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding CardInfo}">
                            <StackPanel>
                                <TextBlock Text="IP адрес:" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding IpAddress}" Margin="0,0,0,8"/>

                                <TextBlock Text="Индекс:" Margin="0,0,0,3"/>
                                <hc:NumericUpDown Value="{Binding SeqIndex}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                <TextBlock Text="Комментарий:" Margin="0,0,0,3"/>
                                <TextBox Text="{Binding Comment}" Margin="0,0,0,8"/>
                            </StackPanel>
                        </Border>

                        <!-- ScannerConfig -->
                        <TextBlock Text="Конфигурация сканатора" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ScannerConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="FieldSizeX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FieldSizeX}" Minimum="0" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="FieldSizeY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FieldSizeY}" Minimum="0" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="ProtocolCode:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ProtocolCode}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                    <TextBlock Text="CoordinateTypeCode:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CoordinateTypeCode}" Minimum="0" Maximum="10" Margin="0,0,0,8"/>

                                    <TextBlock Text="OffsetX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetX}" Minimum="-1000" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="OffsetY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetY}" Minimum="-1000" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="OffsetZ:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding OffsetZ}" Minimum="-1000" Maximum="1000" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="ScaleX:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleX}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="ScaleY:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleY}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="ScaleZ:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding ScaleZ}" Minimum="0" Maximum="10" DecimalPlaces="3" Margin="0,0,0,8"/>

                                    <TextBlock Text="RotateAngle:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding RotateAngle}" Minimum="-360" Maximum="360" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- BeamConfig -->
                        <TextBlock Text="Конфигурация луча" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding BeamConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="MinBeamDiameterMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MinBeamDiameterMicron}" Minimum="0" Maximum="1000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="WavelengthNano:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding WavelengthNano}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="RayleighLengthMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding RayleighLengthMicron}" Minimum="0" Maximum="100000" DecimalPlaces="1" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="M2:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding M2}" Minimum="0" Maximum="10" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <TextBlock Text="FocalLengthMm:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding FocalLengthMm}" Minimum="0" Maximum="10000" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- LaserPowerConfig -->
                        <TextBlock Text="Конфигурация мощности лазера" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding LaserPowerConfig}">
                            <StackPanel>
                                <TextBlock Text="MaxPower:" Margin="0,0,0,3"/>
                                <hc:NumericUpDown Value="{Binding MaxPower}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                <TextBlock Text="PowerOffsetKFactor:" Margin="0,0,0,3"/>
                                <hc:NumericUpDown Value="{Binding PowerOffsetKFactor}" Minimum="-100" Maximum="100" DecimalPlaces="6" Margin="0,0,0,8"/>

                                <TextBlock Text="PowerOffsetCFactor:" Margin="0,0,0,3"/>
                                <hc:NumericUpDown Value="{Binding PowerOffsetCFactor}" Minimum="-1000" Maximum="1000" DecimalPlaces="6" Margin="0,0,0,8"/>
                            </StackPanel>
                        </Border>

                        <!-- ThirdAxisConfig -->
                        <TextBlock Text="Конфигурация третьей оси" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ThirdAxisConfig}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="AFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Afactor}" Minimum="-100" Maximum="100" DecimalPlaces="6" Margin="0,0,0,8"/>

                                    <TextBlock Text="BFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Bfactor}" Minimum="-100" Maximum="100" DecimalPlaces="10" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="CFactor:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Cfactor}" Minimum="-100" Maximum="100" DecimalPlaces="14" Margin="0,0,0,8"/>

                                    <TextBlock Text="BaseFocal:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding BaseFocal}" Minimum="0" Maximum="10000" DecimalPlaces="2" Margin="0,0,0,8"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- FunctionSwitcherConfig -->
                        <TextBlock Text="Переключатели функций" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15">
                            <Grid DataContext="{Binding FunctionSwitcherConfig}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Коррекция мощности (Offset)" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="0" Grid.Column="1" IsChecked="{Binding EnablePowerOffset}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Коррекция мощности (Correction)" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="1" Grid.Column="1" IsChecked="{Binding EnablePowerCorrection}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Z коррекция" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="2" Grid.Column="1" IsChecked="{Binding EnableZCorrection}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Изменение диаметра пучка" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="3" Grid.Column="1" IsChecked="{Binding EnableDiameterChange}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Динамическая смена переменных" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="4" Grid.Column="1" IsChecked="{Binding EnableDynamicChangeVariables}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="5" Grid.Column="0" Text="Ограничение Min точки" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="5" Grid.Column="1" IsChecked="{Binding LimitVariablesMinPoint}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="6" Grid.Column="0" Text="Ограничение Max точки" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="6" Grid.Column="1" IsChecked="{Binding LimitVariablesMaxPoint}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>

                                <TextBlock Grid.Row="7" Grid.Column="0" Text="Переменная задержка прыжка" VerticalAlignment="Center" Margin="0,5"/>
                                <ToggleButton Grid.Row="7" Grid.Column="1" IsChecked="{Binding EnableVariableJumpDelay}" Style="{StaticResource ToggleButtonSwitch}" Margin="5"/>
                            </Grid>
                        </Border>

                        <!-- NonDepends ProcessVariables -->
                        <TextBlock Text="Базовые параметры процесса (NonDepends)" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,10"/>
                        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15"
                                DataContext="{Binding ProcessVariablesMap.NonDepends[0]}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="MarkSpeed:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MarkSpeed}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpSpeed:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpSpeed}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="PolygonDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding PolygonDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="MarkDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MarkDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOnDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOnDelay}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOffDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOffDelay}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="LaserOnDelay (SkyWriting):" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOnDelayForSkyWriting}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="LaserOffDelay (SkyWriting):" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding LaserOffDelayForSkyWriting}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="CurBeamDiameterMicron:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CurBeamDiameterMicron}" Minimum="0" Maximum="1000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="CurPower:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding CurPower}" Minimum="0" Maximum="1000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="JumpMaxLengthLimitMm:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding JumpMaxLengthLimitMm}" Minimum="0" Maximum="10000" DecimalPlaces="1" Margin="0,0,0,8"/>

                                    <TextBlock Text="MinJumpDelay:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding MinJumpDelay}" Minimum="0" Maximum="100000" Margin="0,0,0,8"/>

                                    <TextBlock Text="Umax:" Margin="0,0,0,3"/>
                                    <hc:NumericUpDown Value="{Binding Umax}" Minimum="0" Maximum="10" DecimalPlaces="2" Margin="0,0,0,8"/>

                                    <CheckBox Content="SkyWriting Enable" IsChecked="{Binding Swenable}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- MarkSpeed Profiles -->
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Профили MarkSpeed" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Добавить профиль"
                                    Command="{Binding DataContext.AddMarkSpeedProfile2Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Style="{StaticResource ButtonSuccess}"
                                    Margin="5,0,0,0"/>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding DataContext.Scanner2MarkSpeedProfiles, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <Grid Grid.Row="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding MarkSpeed, StringFormat='Скорость: {0} мм/с'}"
                                                           FontWeight="Bold" VerticalAlignment="Center"/>
                                                <Button Grid.Column="1" Content="Редактировать"
                                                        Command="{Binding DataContext.EditMarkSpeedProfile2Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource ButtonInfo}"
                                                        Margin="5,0"/>
                                                <Button Grid.Column="2" Content="Удалить"
                                                        Command="{Binding DataContext.DeleteMarkSpeedProfile2Command, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"
                                                        Style="{StaticResource ButtonDanger}"/>
                                            </Grid>

                                            <Expander Grid.Row="1" Header="Детали" Margin="0,10,0,0">
                                                <Grid Margin="10">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                                        <TextBlock Text="{Binding JumpSpeed, StringFormat='JumpSpeed: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding PolygonDelay, StringFormat='PolygonDelay: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding JumpDelay, StringFormat='JumpDelay: {0}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding MarkDelay, StringFormat='MarkDelay: {0}'}" Margin="0,2"/>
                                                    </StackPanel>

                                                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                                        <TextBlock Text="{Binding LaserOnDelay, StringFormat='LaserOnDelay: {0:F1}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding LaserOffDelay, StringFormat='LaserOffDelay: {0:F1}'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding CurBeamDiameterMicron, StringFormat='BeamDiameter: {0:F1} μm'}" Margin="0,2"/>
                                                        <TextBlock Text="{Binding CurPower, StringFormat='Power: {0:F1} W'}" Margin="0,2"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Expander>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="5" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent" ShowsPreview="True"/>

            <!-- Правая панель: Обнаруженные сканаторы -->
            <Border Grid.Column="6" Background="White" CornerRadius="10" Margin="8,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="#20000000" BlurRadius="15" ShadowDepth="3" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок секции -->
                    <Border Grid.Row="0" CornerRadius="10,10,0,0" Padding="15,12">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#8B5CF6" Offset="0"/>
                                <GradientStop Color="#7C3AED" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <TextBlock Text="🔍 Обнаруженные сканаторы" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DiscoveredScanners}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <hc:Card Margin="0,0,0,10" Padding="10" BorderBrush="Transparent" BorderThickness="1">
                                        <Border CornerRadius="10" Margin="10" Padding="15" Background="Black">
                                            <StackPanel>
                                                <TextBlock Foreground="White" Text="{Binding Address}" FontWeight="Bold" FontSize="13" Margin="0,0,0,5"/>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Статус: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding ConnectState}" FontSize="11"/>
                                                </StackPanel>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Index: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding Index}" FontSize="11"/>
                                                </StackPanel>

                                                <Separator Margin="0,5"/>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Загрузка: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding StreamProgress}" FontSize="11"/>
                                                    <TextBlock Text="%" FontSize="11"/>
                                                </StackPanel>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Завершено: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding StreamEnd}" FontSize="11"/>
                                                </StackPanel>

                                                <Separator Margin="0,5"/>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Маркировка: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding MarkingProgress}" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="%" FontSize="11"/>
                                                </StackPanel>

                                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                                    <TextBlock Foreground="Gray" Text="Завершено: " FontWeight="SemiBold" FontSize="11"/>
                                                    <TextBlock Foreground="White" Text="{Binding MarkComplete}" FontSize="11"/>
                                                </StackPanel>

                                                <Button IsEnabled="{Binding ConnectButtonIsEnabled}" Command="{Binding Connect}" CommandParameter="{Binding }"  Margin="0 20 0 5" HorizontalAlignment="Stretch" Style="{StaticResource ButtonSuccess}" Content="Подключиться"/>
                                                <Button IsEnabled="{Binding DisconnectButtonIsEnabled}" Command="{Binding Disconnect}" CommandParameter="{Binding }" HorizontalAlignment="Stretch" Style="{StaticResource ButtonDanger}"  Content="Отключиться"/>

                                            </StackPanel>
                                        </Border>

                                    </hc:Card>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        </Grid>
    </Grid>
</UserControl>
