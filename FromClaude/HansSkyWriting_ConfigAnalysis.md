# Откуда PrintMateMC берет параметры SkyWriting

## 📋 Ваша конфигурация (scanner config JSON)

```json
{
  "processVariablesMap": {
    "markSpeed": [
      {
        "markSpeed": 800,
        "swenable": true,       // ← enable для SkyWriting
        "umax": 0.1             // ← uniformLen для SkyWriting
      }
    ]
  }
}
```

## ❗ Проблема: Недостающие параметры

Для полного вызова `UDM_SetSkyWritingMode` нужно **5 параметров**:

```csharp
UDM_SetSkyWritingMode(
    int enable,        // ← ЕСТЬ в конфигурации (swenable)
    int mode,          // ← НЕТ в конфигурации!
    float uniformLen,  // ← ЕСТЬ в конфигурации (umax)
    float accLen,      // ← НЕТ в конфигурации!
    float angleLimit   // ← НЕТ в конфигурации!
);
```

### Таблица параметров:

| Параметр UDM API | В вашей конфигурации? | Источник | Значение |
|------------------|-----------------------|----------|----------|
| `enable` | ✅ **ЕСТЬ** | `swenable` | `true` → 1 |
| `mode` | ❌ **НЕТ** | Хардкод | 0 |
| `uniformLen` | ✅ **ЕСТЬ** | `umax` | 0.1 mm |
| `accLen` | ❌ **НЕТ** | Эвристика | `uniformLen * 0.5` = 0.05 mm |
| `angleLimit` | ❌ **НЕТ** | Хардкод | 120.0° |

---

## 🔍 Как Hans4Java решает проблему

Hans4Java (используемый в PrintMateMC) **НЕ вызывает** `UDM_SetSkyWritingMode` напрямую.

### Высокоуровневая обертка:

```java
// PrintMateMC Java код:
new SWEnableOperation(true)   // ← Только включить/выключить!
```

**Что происходит внутри Hans4Java:**

```java
// Псевдокод внутри Hans4Java
class SWEnableOperation {
    public SWEnableOperation(boolean enable) {
        // Прочитать из конфигурации
        ScannerConfig config = loadConfig();

        int enableInt = enable ? 1 : 0;
        int mode = 0;  // ← Хардкод
        float uniformLen = config.umax;  // ← Из конфигурации (0.1)
        float accLen = uniformLen * 0.5f;  // ← Хардкод формула (0.05)
        float angleLimit = 120.0f;  // ← Хардкод (default)

        // Вызов низкоуровневого API
        UDM_SetSkyWritingMode(enableInt, mode, uniformLen, accLen, angleLimit);
    }
}
```

---

## 📊 Анализ вашей конфигурации

### Лазер 1 (IP: 172.18.34.227, seqIndex: 0)

| Speed (mm/s) | swenable | umax | Рекомендуемый accLen | Рекомендуемый angleLimit |
|--------------|----------|------|----------------------|--------------------------|
| 800 | true | 0.1 | 0.05 | 120.0 |
| 1250 | true | 0.1 | 0.05 | 120.0 |
| 2000 | true | 0.1 | 0.05 | 120.0 |

### Лазер 2 (IP: 172.18.34.228, seqIndex: 1)

| Speed (mm/s) | swenable | umax | Рекомендуемый accLen | Рекомендуемый angleLimit |
|--------------|----------|------|----------------------|--------------------------|
| 800 | true | 0.1 | 0.05 | 120.0 |
| 1250 | true | 0.1 | 0.05 | 120.0 |
| 2000 | true | 0.1 | 0.05 | 120.0 |

### 🔥 Важное наблюдение:

**ВСЕ** скорости в вашей конфигурации имеют **одинаковые** параметры SkyWriting:
- `swenable = true`
- `umax = 0.1`

Это означает, что вы можете использовать **фиксированные значения** вместо динамического выбора по скорости!

---

## ✅ Рекомендованное решение для C#

### Вариант 1: Упрощенный (фиксированные параметры)

Поскольку все скорости одинаковые, используйте **константы**:

```csharp
// Для ВСЕХ регионов и скоростей
HM_UDM_DLL.UDM_SetSkyWritingMode(
    enable: 1,              // swenable: true (из конфигурации)
    mode: 0,                // Всегда 0
    uniformLen: 0.1f,       // umax: 0.1 (из конфигурации)
    accLen: 0.05f,          // = uniformLen / 2 (эвристика)
    angleLimit: 120.0f      // Стандартное значение
);
```

### Вариант 2: Динамический (чтение из JSON)

Если конфигурация может измениться:

```csharp
// Прочитать конфигурацию
string json = File.ReadAllText("scanner_config.json");
var configs = JsonSerializer.Deserialize<ScannerCardConfig[]>(json);

// Первый лазер
var laser1 = configs[0];

// Выбрать параметры для скорости 800 mm/s
var speedConfig = laser1.ProcessVariablesMap.MarkSpeed
    .FirstOrDefault(s => s.MarkSpeed == 800);

// Применить параметры
int enable = speedConfig.SWEnable ? 1 : 0;
float uniformLen = (float)speedConfig.Umax;
float accLen = uniformLen * 0.5f;    // Эвристика
float angleLimit = 120.0f;           // Default

HM_UDM_DLL.UDM_SetSkyWritingMode(enable, 0, uniformLen, accLen, angleLimit);
```

См. полный пример в [HansSkyWriting_FromYourConfig.cs](HansSkyWriting_FromYourConfig.cs).

---

## 🧮 Формулы для расчета недостающих параметров

### 1. `accLen` (длина ускорения)

**Рекомендуемая формула:**
```csharp
float accLen = uniformLen * 0.5f;
```

**Обоснование:**
- Hans4Java использует эту эвристику
- `accLen` должен быть меньше `uniformLen`
- Типичное соотношение: 0.4 - 0.6

**Альтернативные значения:**
```csharp
// Консервативный (менее агрессивный)
float accLen = uniformLen * 0.4f;  // 0.04 mm

// Стандартный
float accLen = uniformLen * 0.5f;  // 0.05 mm

// Агрессивный (более плавный)
float accLen = uniformLen * 0.6f;  // 0.06 mm
```

### 2. `angleLimit` (предельный угол)

**Рекомендуемое значение:**
```csharp
float angleLimit = 120.0f;  // градусы
```

**Обоснование:**
- Стандартное значение в Hans API
- Применяется к большинству углов (0° - 120°)
- Используется в Hans4Java по умолчанию

**Альтернативные значения:**
```csharp
// Более агрессивное сглаживание
float angleLimit = 150.0f;  // Почти все углы

// Стандартное
float angleLimit = 120.0f;

// Консервативное (только плавные кривые)
float angleLimit = 90.0f;
```

### 3. `mode` (режим)

**Всегда:**
```csharp
int mode = 0;
```

**Обоснование:**
- Единственный документированный режим
- Hans4Java всегда использует 0
- Другие значения зарезервированы

---

## 🔬 Специальные задержки для SkyWriting

В вашей конфигурации есть **отдельные задержки** для SkyWriting режима:

```json
{
  "laserOnDelay": 420.0,                    // Обычный режим
  "laserOffDelay": 490.0,                   // Обычный режим
  "laserOnDelayForSkyWriting": 600.0,       // ← Для SkyWriting!
  "laserOffDelayForSkyWriting": 730.0       // ← Для SkyWriting!
}
```

### Когда использовать:

```csharp
// Если SkyWriting ВКЛЮЧЕН
if (skywritingEnabled) {
    layers[0].LaserOnDelay = (float)config.LaserOnDelayForSkyWriting;
    layers[0].LaserOffDelay = (float)config.LaserOffDelayForSkyWriting;
}
// Если SkyWriting ВЫКЛЮЧЕН
else {
    layers[0].LaserOnDelay = (float)config.LaserOnDelay;
    layers[0].LaserOffDelay = (float)config.LaserOffDelay;
}
```

### Почему разные задержки?

- **С SkyWriting**: Лазер остается включенным дольше → нужны большие задержки для стабилизации
- **Без SkyWriting**: Лазер быстро включается/выключается → меньшие задержки

---

## 📈 Сравнение двух лазеров

### Параметры SkyWriting:

| Параметр | Лазер 1 (227) | Лазер 2 (228) | Отличия |
|----------|---------------|---------------|---------|
| `swenable` | true | true | ✅ Одинаковые |
| `umax` | 0.1 | 0.1 | ✅ Одинаковые |
| `accLen` (calculated) | 0.05 | 0.05 | ✅ Одинаковые |
| `angleLimit` (default) | 120.0 | 120.0 | ✅ Одинаковые |

### Другие отличия:

| Параметр | Лазер 1 (227) | Лазер 2 (228) | Комментарий |
|----------|---------------|---------------|-------------|
| `jumpDelay` | 40000 ns | 35000 ns | ⚠️ Лазер 2 быстрее |
| `minBeamDiameterMicron` | 58.91 μm | 66.8 μm | ⚠️ Разная оптика |
| `offsetZ` | -0.08 mm | -0.067 mm | ⚠️ Разная калибровка |

**Вывод:** Параметры SkyWriting **одинаковые**, но другие параметры отличаются.

---

## 🎯 Итоговая рекомендация

### Для ВАШЕЙ конфигурации используйте:

```csharp
public static void ApplySkyWriting()
{
    HM_UDM_DLL.UDM_NewFile();
    HM_UDM_DLL.UDM_SetProtocol(0, 1);

    // Параметры из вашей конфигурации
    HM_UDM_DLL.UDM_SetSkyWritingMode(
        enable: 1,              // swenable: true
        mode: 0,                // Всегда 0
        uniformLen: 0.1f,       // umax: 0.1
        accLen: 0.05f,          // = uniformLen / 2
        angleLimit: 120.0f      // Стандартное значение
    );

    // Параметры слоя с задержками ДЛЯ SKYWRITING
    MarkParameter[] layers = new MarkParameter[1];
    layers[0] = new MarkParameter
    {
        MarkSpeed = 800,
        LaserPower = 50.0f,
        JumpSpeed = 25000,
        LaserOnDelay = 600.0f,   // ← laserOnDelayForSkyWriting
        LaserOffDelay = 730.0f,  // ← laserOffDelayForSkyWriting
        MarkDelay = 470,
        JumpDelay = 40000,       // Для Лазера 1
        PolygonDelay = 385,
        MarkCount = 1
    };

    HM_UDM_DLL.UDM_SetLayersPara(layers, 1);

    // Добавить геометрию...
}
```

---

## 📚 Связанные файлы

- [HansSkyWriting_FromYourConfig.cs](HansSkyWriting_FromYourConfig.cs) - Примеры с вашей конфигурацией
- [HansSkyWritingMode_CliExamples.cs](HansSkyWritingMode_CliExamples.cs) - Общие примеры
- [HansSkyWritingMode_README.md](HansSkyWritingMode_README.md) - Документация параметров
- [HansSkyWriting_JavaUsage_Analysis.md](HansSkyWriting_JavaUsage_Analysis.md) - Анализ Java кода

---

## ❓ FAQ

### Q: Почему в конфигурации нет `accLen` и `angleLimit`?

**A:** Эти параметры считаются "низкоуровневыми" и обычно не меняются. Hans4Java использует разумные defaults:
- `accLen = uniformLen * 0.5`
- `angleLimit = 120.0°`

### Q: Можно ли изменить `accLen` и `angleLimit`?

**A:** Да, вы можете экспериментировать:
- Увеличьте `accLen` для более плавного ускорения
- Уменьшите `angleLimit` чтобы SkyWriting применялся только к плавным кривым

### Q: Почему все скорости имеют одинаковый `umax = 0.1`?

**A:** Возможно, ваш материал/процесс не требует адаптации по скорости. Типично для стабильных процессов.

### Q: Нужно ли использовать разные параметры для Лазера 1 и Лазера 2?

**A:** Нет, параметры SkyWriting одинаковые. Но учитывайте различия в `jumpDelay` и `offsetZ`.

---

## 🔄 Workflow применения SkyWriting

```
1. Прочитать scanner_config.json
   ↓
2. Извлечь параметры для нужной скорости:
   - swenable → enable
   - umax → uniformLen
   ↓
3. Рассчитать недостающие параметры:
   - accLen = uniformLen * 0.5
   - angleLimit = 120.0
   - mode = 0
   ↓
4. Вызвать UDM_SetSkyWritingMode(enable, mode, uniformLen, accLen, angleLimit)
   ↓
5. Использовать специальные задержки:
   - laserOnDelayForSkyWriting
   - laserOffDelayForSkyWriting
   ↓
6. Добавить геометрию
   ↓
7. Сохранить файл
```

---

**Главный вывод:** Ваша конфигурация содержит **только 2 из 5** параметров (`swenable` и `umax`). Остальные 3 параметра (`mode`, `accLen`, `angleLimit`) нужно **либо хардкодить**, **либо вычислять** по формулам выше.
