# Ваша система: F-theta 538.46 мм

## 🎯 Формула для ВАШЕЙ системы:

```csharp
// Параметры для F-theta 538.46 мм
const double NOMINAL_DIAMETER = 120.0;  // μm при Z=0
const double Z_COEFFICIENT = 0.3;       // мм на 10 μm

// Формула
float z = (float)((diameter - 120.0) / 10.0 * 0.3);
```

## 📊 Откуда числа:

| Параметр | Значение | Объяснение |
|----------|----------|------------|
| **120.0** | Номинальный диаметр | Для линзы 538 мм типичный диаметр: 120-150 μm |
| **10.0** | Делитель | Константа (переводит μm в "десятки μm") |
| **0.3** | Коэффициент Z | Для больших линз коэффициент выше: 0.2-0.4 |

### Почему 120 μm?

Для больших F-theta линз диаметр пятна больше:
- F-theta 100 мм → диаметр ~50 μm
- F-theta 160 мм → диаметр ~70 μm
- F-theta 254 мм → диаметр ~100 μm
- **F-theta 538 мм → диаметр ~120 μm** ← ВАША

### Почему 0.3?

Для больших линз луч расходится медленнее:
- Короткая линза (100 мм): коэффициент ~0.05
- Средняя линза (160 мм): коэффициент ~0.1
- Длинная линза (254 мм): коэффициент ~0.2
- **Очень длинная (538 мм): коэффициент ~0.3** ← ВАША

---

## 💻 Готовый код (копируй-вставляй):

```csharp
using Hans.NET;

public class MySystem
{
    // Параметры ДЛЯ ВАШЕЙ системы (F-theta 538.46 мм)
    const double NOMINAL_DIAMETER = 120.0;  // μm
    const double Z_COEFFICIENT = 0.3;       // мм/10μm

    public static void ApplyDiameter(double diameter)
    {
        // Конвертация диаметра в Z
        float z = (float)((diameter - NOMINAL_DIAMETER) / 10.0 * Z_COEFFICIENT);

        // Инициализация
        HM_UDM_DLL.UDM_NewFile();
        HM_UDM_DLL.UDM_SetProtocol(0, 1); // 3D режим!

        // Параметры
        MarkParameter[] layers = new MarkParameter[1];
        layers[0] = new MarkParameter
        {
            MarkSpeed = 800,
            LaserPower = 50.0f,
            // ... остальные параметры
        };
        HM_UDM_DLL.UDM_SetLayersPara(layers, 1);

        // Геометрия с Z
        structUdmPos[] points = new structUdmPos[]
        {
            new structUdmPos { x = 0, y = 0, z = z }, // ← Z применяет диаметр!
            new structUdmPos { x = 10, y = 0, z = z }
        };
        HM_UDM_DLL.UDM_AddPolyline3D(points, 2, 0);

        // Сохранение
        HM_UDM_DLL.UDM_Main();
        HM_UDM_DLL.UDM_SaveToFile($"diameter_{diameter}um.bin");
        HM_UDM_DLL.UDM_EndMain();
    }

    // Пример: Диаметр 80 μm из CLI
    public static void Main()
    {
        ApplyDiameter(80.0); // ← Ваш диаметр из CLI
    }
}
```

---

## 📋 Таблица: Диаметр → Z для вашей системы

| Диаметр (μm) | Z (мм) | Назначение |
|--------------|--------|------------|
| 60 | -1.800 | Невозможно (слишком малый) |
| 80 | -1.200 | Тонкие детали |
| 100 | -0.600 | Контур высокой точности |
| **120** | **0.000** | **Номинальный (Z=0)** |
| 140 | +0.600 | Downskin, Upskin |
| 160 | +1.200 | Infill (заполнение) |
| 180 | +1.800 | Поддержки |
| 200 | +2.400 | Грубая печать |

**⚠️ Рекомендуемый диапазон Z: ±1.2 мм**

---

## 🔬 Калибровка (если нужна точность)

### Быстрая калибровка (10 минут):

1. **Напечатайте 3 линии:**
   ```csharp
   // Линия 1: Z = -0.6 мм
   // Линия 2: Z = 0.0 мм
   // Линия 3: Z = +0.6 мм
   ```

2. **Измерьте ширину под микроскопом:**
   ```
   Линия 1: _____ μm
   Линия 2: _____ μm  ← Это ваш номинальный диаметр!
   Линия 3: _____ μm
   ```

3. **Рассчитайте коэффициент:**
   ```
   Номинальный диаметр = ширина линии 2
   ΔZ = 0.6 мм
   Δdiameter = ширина линии 3 - ширина линии 2
   Коэффициент = ΔZ / (Δdiameter / 10)
   ```

4. **Обновите константы в коде:**
   ```csharp
   const double NOMINAL_DIAMETER = ваше_измерение;
   const double Z_COEFFICIENT = ваш_коэффициент;
   ```

---

## 🚀 Примеры для разных регионов CLI

```csharp
// Edges (тонкие края)
float z_edges = (float)((65.0 - 120.0) / 10.0 * 0.3);  // z = -1.65 мм
// ⚠️ Слишком малый диаметр! Используйте минимум 100 μm

// Контур (Contour)
float z_contour = (float)((100.0 - 120.0) / 10.0 * 0.3);  // z = -0.6 мм

// Upskin/Downskin
float z_skin = (float)((140.0 - 120.0) / 10.0 * 0.3);  // z = +0.6 мм

// Infill (заполнение)
float z_infill = (float)((160.0 - 120.0) / 10.0 * 0.3);  // z = +1.2 мм

// Support (поддержки)
float z_support = (float)((180.0 - 120.0) / 10.0 * 0.3);  // z = +1.8 мм
```

---

## ⚠️ ВАЖНО для вашей системы:

### 1. Минимальный диаметр ограничен
Для линзы 538 мм сложно получить диаметр меньше 100 μm:
- CLI параметр 60-80 μm → используйте 100 μm (Z = -0.6 мм)
- CLI параметр 80-100 μm → используйте 100-120 μm (Z = -0.6 до 0 мм)

### 2. Рабочий диапазон Z
Оптимальный диапазон: **Z от -0.6 до +1.8 мм**
- За пределами этого диапазона может быть сильная аберрация

### 3. Рекомендуемые диаметры для регионов:

| Регион CLI | Диаметр из CLI | Адаптированный для 538 мм | Z (мм) |
|------------|----------------|---------------------------|--------|
| edges | 65 μm | **100 μm** (минимум) | -0.6 |
| contour | 70 μm | **110 μm** | -0.3 |
| upskin/downskin | 80 μm | **140 μm** | +0.6 |
| infill | 90 μm | **160 μm** | +1.2 |
| support | 100 μm | **180 μm** | +1.8 |

---

## 📁 Полный пример в файле:

**[YOUR_SYSTEM_538mm.cs](YOUR_SYSTEM_538mm.cs)** - готовый код с:
- Таблицей конвертации
- Примерами для всех регионов
- Программой автоматической калибровки

**Запустите его:** `dotnet run`

---

## 🎯 Итоговая формула (еще раз):

```csharp
// ДЛЯ ВАШЕЙ СИСТЕМЫ F-theta 538.46 мм:
float z = (float)((diameter - 120.0) / 10.0 * 0.3);

// Пример: diameter = 80 μm
// НО! 80 μm слишком мало для линзы 538 мм
// Используйте адаптированный диаметр 140 μm:
float z = (float)((140.0 - 120.0) / 10.0 * 0.3);
// z = 0.6 мм
```

**Готово!** 🚀
