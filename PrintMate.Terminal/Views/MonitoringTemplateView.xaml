<UserControl x:Class="PrintMate.Terminal.Views.MonitoringTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="1045" d:DesignWidth="1474">

        <StackPanel>



            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="1" HorizontalAlignment="Center" Margin="0 50 0 0" x:Name="AnimatedTextContainer">
                    <TextBlock x:Name="OldTextBlock"
                               FontSize="40" FontWeight="Black" Foreground="#212121"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Opacity="1">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                    <TextBlock x:Name="NewTextBlock"
                               FontSize="40" FontWeight="Black" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-5"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Grid>
            <Border Grid.Column="2" Margin="0 0 50 0" VerticalAlignment="Center"> 
                <WrapPanel HorizontalAlignment="Right">

                    <TextBlock FontWeight="Bold" FontSize="15" Foreground="White" VerticalAlignment="Center" Margin="5" Text="Количество колонок"/>

                    <ListBox ItemsSource="{Binding ColModes}"
                             SelectedItem="{Binding SelectedColMode}"
                             HorizontalAlignment="Right"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled">

                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="ItemBorder"
                                        Padding="5"
                                        Background="#2c2c2c"
                                        CornerRadius="5"
                                        Margin="5">
                                    <Image Width="30" Source="{Binding ImageSource}" />
                                </Border>
                                <DataTemplate.Triggers>
                                    <DataTrigger 
                                        Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" 
                                        Value="True">
                                        <Setter TargetName="ItemBorder" Property="Background" Value="#ff4500" />
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <!-- Убираем белый фон и системное выделение -->
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </WrapPanel>



            </Border>
        </Grid>



        <Border Margin="10" Background="Transparent" CornerRadius="15" HorizontalAlignment="Center">
            <ListBox ItemsSource="{Binding Groups}"
             SelectedItem="{Binding SelectedGroup}"
             HorizontalAlignment="Center"
             Background="Transparent"
             BorderThickness="0"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             ScrollViewer.VerticalScrollBarVisibility="Disabled">

                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid HorizontalAlignment="Center" Columns="5"></UniformGrid>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Border MinWidth="100" x:Name="ItemBorder" Background="#161616" Padding="10" Margin="5" CornerRadius="15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Border Background="Black" Padding="10" CornerRadius="10">
                                        <Image Width="30" Source="{Binding ImagePath}" />
                                    </Border>
                                    <TextBlock Grid.Column="1" Margin="20 0 20 0 " FontSize="12" FontWeight="Black" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" Text="{Binding Name}"/>

                                </Grid>


                            </Border>
                            <Border HorizontalAlignment="Center" x:Name="SelectedLine" Visibility="Collapsed"  Background="#ff4500" Width="190" Height="3" Margin="0 2 0 0"/>

                        </StackPanel>
                        <DataTemplate.Triggers>
                            <DataTrigger 
                        Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" 
                        Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#212121" />
                                <Setter TargetName="SelectedLine" Property="Visibility" Value="Visible" />

                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <!-- Убираем белый фон и системное выделение -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>



        </Border>



        <Border Height="750" Padding="15" ClipToBounds="True">
                <ScrollViewer x:Name="ScrollViewer"
                              PanningMode="VerticalOnly" ManipulationBoundaryFeedback="ScrollViewer_ManipulationBoundaryFeedback"
                              VerticalScrollBarVisibility="Auto">
                    <Border x:Name="MainBorder" />
                </ScrollViewer>
            </Border>
    </StackPanel>
</UserControl>
