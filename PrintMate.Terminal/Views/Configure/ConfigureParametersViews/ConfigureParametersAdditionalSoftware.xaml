<UserControl x:Class="PrintMate.Terminal.Views.Configure.ConfigureParametersViews.ConfigureParametersAdditionalSoftware"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views.Configure.ConfigureParametersViews"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border x:Name="TestBorder" Background="#191919" Padding="15">
            <StackPanel>
                <TextBlock Margin="15 0 0 0" FontSize="20" HorizontalAlignment="Left" FontWeight="Bold" Grid.Column="0" TextOptions.TextFormattingMode="Display"
                           TextOptions.TextRenderingMode="ClearType"
                           RenderOptions.ClearTypeHint="Enabled" Foreground="White" Text="Название"/>
            </StackPanel>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" Margin="0 0 0 0" ManipulationBoundaryFeedback="ScrollViewer_ManipulationBoundaryFeedback">
            <StackPanel>
                <Border Background="#191919">
                    <ListBox ItemsSource="{Binding Packages}"
                         Margin="0 0 0 0"
                         BorderThickness="0"
                         VerticalAlignment="Top"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                        >

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border CornerRadius="5" x:Name="itemBorder" Padding="20" BorderThickness="1" BorderBrush="#292929" Margin="2">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <!--<ColumnDefinition />-->
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock HorizontalAlignment="Left" Grid.Column="0" Foreground="White" FontWeight="DemiBold" FontSize="15" Text="{Binding Name}"/>
                                                    <!--<TextBlock HorizontalAlignment="Left"  Grid.Column="1" Foreground="White" FontWeight="DemiBold" FontSize="15" Text="{Binding Name}"/>-->
                                                </Grid>
                                            </Border>

                                            <!--<ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="itemBorder" Property="Background" Value="#ff4500"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                </Trigger>
                                            </ControlTemplate.Triggers>-->
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.Template>
                            <ControlTemplate TargetType="ListBox">
                                <ItemsPresenter />
                            </ControlTemplate>
                        </ListBox.Template>
                    </ListBox>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
