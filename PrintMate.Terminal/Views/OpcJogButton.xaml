<UserControl x:Class="PrintMate.Terminal.Views.OpcJogButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views"

             TouchDown="OpcJogButton_OnTouchDown"
             TouchUp="OpcJogButton_OnTouchUp"
             MouseDown="OpcJogButton_OnMouseDown"
             MouseUp="OpcJogButton_OnMouseUp"
             mc:Ignorable="d">

    <Border x:Name="MainBorder" Margin="1" Padding="5" Background="#161616" CornerRadius="5" HorizontalAlignment="Stretch" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1" />
        </Border.RenderTransform>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>

            </Grid.ColumnDefinitions>

            <TextBlock x:Name="LeftTextBlock" Visibility="Collapsed" Grid.Column="1" TextWrapping="Wrap" HorizontalAlignment="Left" FontSize="15" FontWeight="Bold" Foreground="White"  Margin="15 0 15 0" VerticalAlignment="Center" Text="{Binding Cmd.Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/>



            <Border Grid.Column="0" Visibility="Collapsed" x:Name="LeftImageBlock" Background="Black" Padding="15" CornerRadius="5">
                <Image x:Name="LeftImage" Width="30" Height="30"/>
            </Border>


            <TextBlock x:Name="RightTextBlock" Visibility="Collapsed" Grid.Column="1" TextWrapping="Wrap" HorizontalAlignment="Left" FontSize="15" FontWeight="Bold" Foreground="White"  Margin="15 0 15 0" VerticalAlignment="Center" Text="{Binding Cmd.Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/>


            <Border Grid.Column="2" Visibility="Collapsed" x:Name="RightImageBlock" Background="Black" Padding="15" CornerRadius="5">
                <Image x:Name="RightImage" Width="30" Height="30"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
