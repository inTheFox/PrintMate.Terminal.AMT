<UserControl x:Class="PrintMate.Terminal.Views.LoginScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views"
             xmlns:componentsViews="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d" 
             Background="Black"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border Background="Transparent" Width="500">
            <StackPanel>
                <TextBlock Margin="0 0 0 50" HorizontalAlignment="Center" Foreground="White" FontSize="40" FontWeight="Black">Авторизация</TextBlock>

                <componentsViews:EditboxKeyboard Margin="10" Label="Введите логин" Value="{Binding Login, Mode=TwoWay}"/>
                <componentsViews:EditboxKeyboard Margin="10" Label="Введите пароль" Value="{Binding Password, Mode=TwoWay}"/>
                <Button HorizontalAlignment="Stretch" Margin="50" Height="50" Content="Войти"></Button>

                <!--<TextBlock HorizontalAlignment="Left" Foreground="White" FontSize="20" FontWeight="Bold">Введите логин</TextBlock>
                <Border Padding="15" Background="#262626" CornerRadius="15" Margin="0 15 0 0">
                    <TextBlock HorizontalAlignment="Left" Foreground="White" FontSize="20" FontWeight="Medium">Логин</TextBlock>
                </Border>

                <TextBlock HorizontalAlignment="Left" Foreground="White" FontSize="15" FontWeight="Black" Margin="0 30 0 0">Логин</TextBlock>
                <Border Padding="15" Background="#262626" CornerRadius="15">
                    <TextBlock HorizontalAlignment="Left" Foreground="White" FontSize="20" FontWeight="Medium">Логин</TextBlock>
                </Border>-->

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
