<UserControl x:Class="PrintMate.Terminal.Views.ManualControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PrintMate.Terminal.Views"
             xmlns:regions="http://prismlibrary.com/"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:region="clr-namespace:PrintMate.Terminal.Region"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="724">

    <UserControl.Resources>
        <!-- Animation -->
        <Storyboard x:Key="ShowMainView">
            <DoubleAnimation Storyboard.TargetName="mainContainer"
                             Storyboard.TargetProperty="Opacity"
                             To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <QuadraticEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Sidebar Container Style -->
        <Style x:Key="SidebarContainerStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#050505" Offset="0"/>
                        <GradientStop Color="#030303" Offset="0.5"/>
                        <GradientStop Color="#040404" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="3"/>
        </Style>

        <!-- Navigation Item Style -->
        <Style x:Key="NavItemStyle" TargetType="ListBoxItem">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="itemBorder"
                                Margin="0 0 0 5"
                                Padding="8"
                                CornerRadius="10"
                                Cursor="Hand">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop x:Name="bgStart" Color="#0f0f0f" Offset="0"/>
                                    <GradientStop x:Name="bgEnd" Color="#080808" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1a1a1a" Offset="0"/>
                                    <GradientStop Color="#101010" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>

                            <StackPanel HorizontalAlignment="Center">
                                <!-- Icon Container with Glow -->
                                <Grid Width="36" Height="36" HorizontalAlignment="Center">
                                    <!-- Glow Background -->
                                    <Border x:Name="iconGlow" CornerRadius="9" Opacity="0">
                                        <Border.Background>
                                            <RadialGradientBrush>
                                                <GradientStop Color="#40ff6f00" Offset="0"/>
                                                <GradientStop Color="#00ff6f00" Offset="1"/>
                                            </RadialGradientBrush>
                                        </Border.Background>
                                    </Border>

                                    <!-- Icon Background -->
                                    <Border x:Name="iconBg" Width="32" Height="32" CornerRadius="8">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#181818" Offset="0"/>
                                                <GradientStop Color="#0f0f0f" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>

                                        <!-- White Icon (default) -->
                                        <Image x:Name="iconWhite"
                                               Source="{Binding Icon}"
                                               Width="18" Height="18"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                    </Border>

                                    <!-- Colored Icon (selected state) -->
                                    <Border x:Name="iconColoredBg" Width="32" Height="32" CornerRadius="8" Visibility="Collapsed">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#ff6f00" Offset="0"/>
                                                <GradientStop Color="#e65c00" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <Rectangle Width="18" Height="18" Fill="#0a0a0a">
                                            <Rectangle.OpacityMask>
                                                <ImageBrush ImageSource="{Binding Icon}"/>
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </Border>
                                </Grid>

                                <!-- Label -->
                                <TextBlock x:Name="itemText"
                                           Margin="0 5 0 0"
                                           FontWeight="SemiBold"
                                           FontSize="9"
                                           Foreground="#888888"
                                           HorizontalAlignment="Center"
                                           Text="{Binding Name}"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           MaxWidth="60"/>
                            </StackPanel>

                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                    <i:InvokeCommandAction
                                        Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!-- Selected State -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="itemBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#1a1208" Offset="0"/>
                                            <GradientStop Color="#0f0a04" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="itemBorder" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#ff6f00" Offset="0"/>
                                            <GradientStop Color="#cc5500" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="itemBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.4" Color="#ff6f00"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="iconGlow" Property="Opacity" Value="1"/>
                                <Setter TargetName="iconBg" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="iconColoredBg" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="itemText" Property="Foreground" Value="#ff6f00"/>
                                <Setter TargetName="itemText" Property="FontWeight" Value="Bold"/>
                            </Trigger>

                            <!-- Hover State -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="itemBorder" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#181818" Offset="0"/>
                                            <GradientStop Color="#0f0f0f" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="itemBorder" Property="BorderBrush">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#ff6f00" Offset="0"/>
                                            <GradientStop Color="#cc5500" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="itemBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.25" Color="#ff6f00"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="itemText" Property="Foreground" Value="#c0c0c0"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="Grid">
            <Setter Property="Margin" Value="0 0 0 12"/>
        </Style>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource ShowMainView}"/>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid x:Name="mainContainer" ShowGridLines="False" Opacity="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Main Content Area -->
        <region:AnimatedContentControl Grid.Column="0" Margin="0 10 0 0" regions:RegionManager.RegionName="ManualContent"/>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="1" Style="{StaticResource SidebarContainerStyle}" Width="85">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header Section -->
                <Grid Grid.Row="0" Style="{StaticResource SectionHeaderStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Accent Line -->
                    <Border Width="2" Height="12" CornerRadius="1" Margin="0 0 5 0" VerticalAlignment="Center">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#ff6f00" Offset="0"/>
                                <GradientStop Color="#cc5500" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <TextBlock Grid.Column="1"
                               Text="Разделы"
                               FontSize="10"
                               FontWeight="Bold"
                               Foreground="#d0d0d0"
                               VerticalAlignment="Center"/>
                </Grid>

                <!-- Navigation List -->
                <ListBox Grid.Row="1"
                         x:Name="mainViewGrid"
                         ItemsSource="{Binding Items}"
                         SelectedItem="{Binding SelectedItem}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         ItemContainerStyle="{StaticResource NavItemStyle}">
                    <ListBox.Template>
                        <ControlTemplate TargetType="ListBox">
                            <ItemsPresenter/>
                        </ControlTemplate>
                    </ListBox.Template>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
