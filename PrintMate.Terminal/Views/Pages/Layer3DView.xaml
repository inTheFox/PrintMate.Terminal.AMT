<UserControl x:Class="PrintMate.Terminal.Views.Pages.Layer3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920"
             Background="#1E1E1E">

    <UserControl.Resources>
        <!-- Стили для слайдера вращения -->
        <Style x:Key="RotationSliderStyle" TargetType="Slider">
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="20,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 3D Viewport -->
        <hx:Viewport3DX
            Grid.Row="0"
            x:Name="Viewport"
            BackgroundColor="#1E1E1E"
            Camera="{Binding Camera}"
            EffectsManager="{Binding EffectsManager}"
            ShowCoordinateSystem="False"
            ShowCameraInfo="False"
            ShowViewCube="False"
            IsRotationEnabled="False"
            IsZoomEnabled="False"
            IsPanEnabled="False"
            IsInertiaEnabled="False"
            RotateAroundMouseDownPoint="False"
            ZoomAroundMouseDownPoint="False"
            CameraRotationMode="Inspect">

            <!-- Освещение -->
            <hx:DirectionalLight3D Direction="1,1,-1" Color="White"/>
            <hx:DirectionalLight3D Direction="-1,-1,-1" Color="White"/>
            <hx:DirectionalLight3D Direction="0,1,0" Color="#A0A0A0"/>
            <hx:AmbientLight3D Color="#808080"/>

            <!-- Напечатанные слои (solid mesh) -->
            <hx:MeshGeometryModel3D
                Geometry="{Binding PrintedLayersGeometry}"
                Material="{Binding PrintedLayersMaterial}"
                Transform="{Binding ModelTransform}"/>

        </hx:Viewport3DX>

        <!-- Панель управления -->
        <Border Grid.Row="1"
                Background="#2D2D30"
                BorderBrush="#3E3E42"
                BorderThickness="0,1,0,0"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Иконка вращения -->
                <TextBlock Grid.Column="0"
                           Text="🔄"
                           FontSize="24"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"
                           Foreground="White"/>

                <!-- Слайдер вращения -->
                <Slider Grid.Column="1"
                        Style="{StaticResource RotationSliderStyle}"
                        Minimum="0"
                        Maximum="360"
                        Value="{Binding RotationAngle, Mode=TwoWay}"
                        TickFrequency="45"
                        IsSnapToTickEnabled="False"
                        VerticalAlignment="Center"/>

                <!-- Текущий угол -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="15,0,0,0">
                    <TextBlock Text="Угол: "
                               Foreground="#999999"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding RotationAngle, StringFormat={}{0:F0}°}"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Bold"
                               Width="50"
                               VerticalAlignment="Center"/>

                    <!-- Информация о слоях -->
                    <TextBlock Text="  |  Слой: "
                               Foreground="#999999"
                               FontSize="14"
                               Margin="20,0,0,0"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding CurrentLayerIndex}"
                               Foreground="#2196F3"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <TextBlock Text=" / "
                               Foreground="#999999"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding TotalLayers}"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Оверлей загрузки -->
        <Border Grid.Row="0"
                Grid.RowSpan="2"
                Background="#CC000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="⏳"
                           FontSize="48"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>
                <TextBlock Text="{Binding LoadingMessage}"
                           Foreground="White"
                           FontSize="18"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
