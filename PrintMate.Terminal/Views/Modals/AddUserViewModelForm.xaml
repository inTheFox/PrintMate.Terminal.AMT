<UserControl x:Class="PrintMate.Terminal.Views.Modals.AddUserViewModelForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views.Modals"
             xmlns:componentsViews="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d" 
             Width="1024" Height="768"
             d:DesignHeight="768" d:DesignWidth="1024">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border Background="#131313" CornerRadius="10" Padding="15">
            <componentsViews:Container Width="500" HorizontalAlignment="Center" Title="Создание нового пользователя">
                <Border Background="Transparent" Padding="1">
                    <StackPanel>
                        <TextBlock Visibility="{Binding FamilyDangerVisibility}" HorizontalAlignment="Center" Foreground="Red">Это обязательное поле</TextBlock>
                        <componentsViews:EditboxKeyboard Margin="5" Label="Фамилия" Value="{Binding Family, Mode=TwoWay}"/>

                        <TextBlock Visibility="{Binding NameDangerVisibility}" HorizontalAlignment="Center" Foreground="Red">Это обязательное поле</TextBlock>
                        <componentsViews:EditboxKeyboard Margin="5" Label="Имя" Value="{Binding Name, Mode=TwoWay}"/>

                        <TextBlock Visibility="{Binding LoginDangerVisibility}" HorizontalAlignment="Center" Foreground="Red">Это обязательное поле</TextBlock>
                        <componentsViews:EditboxKeyboard Margin="5" Label="Логин" Value="{Binding Login, Mode=TwoWay}"/>

                        <TextBlock Visibility="{Binding PasswordDangerVisibility}" HorizontalAlignment="Center" Foreground="Red">Это обязательное поле</TextBlock>
                        <componentsViews:EditboxKeyboard Margin="5" Label="Пароль" Value="{Binding Password, Mode=TwoWay}"/>

                        <TextBlock Visibility="{Binding RoleDangerVisibility}" HorizontalAlignment="Center" Foreground="Red">Это обязательное поле</TextBlock>
                        <StackPanel Margin="5">
                            <TextBlock Text="Роль" Foreground="White" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding AvailableRoles}"
                                      SelectedItem="{Binding SelectedRole, Mode=TwoWay}"
                                      DisplayMemberPath="DisplayName"
                                      Height="40"
                                      Background="#1a1a1a"
                                      Foreground="White"
                                      BorderBrush="#555"
                                      BorderThickness="1">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem">
                                        <Setter Property="Background" Value="Black"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="Padding" Value="10,5"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBoxItem">
                                                    <Border x:Name="border"
                                                            Background="{TemplateBinding Background}"
                                                            BorderThickness="0"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsHighlighted" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#ff4500"/>
                                                        </Trigger>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="#333333"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                                <ComboBox.Resources>
                                    <Style TargetType="{x:Type Border}" x:Key="DropDownBorderStyle">
                                        <Setter Property="Background" Value="Black"/>
                                        <Setter Property="BorderBrush" Value="#555"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                    </Style>
                                </ComboBox.Resources>
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ComboBox">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="30"/>
                                                        </Grid.ColumnDefinitions>

                                                        <Border Grid.Column="0" Grid.ColumnSpan="2"
                                                                Background="{TemplateBinding Background}"
                                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                                BorderThickness="{TemplateBinding BorderThickness}"/>

                                                        <ContentPresenter x:Name="ContentSite"
                                                                          Grid.Column="0"
                                                                          IsHitTestVisible="False"
                                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                                          Margin="10,0,5,0"
                                                                          VerticalAlignment="Center"
                                                                          HorizontalAlignment="Left" />

                                                        <ToggleButton x:Name="ToggleButton"
                                                                      Grid.Column="1"
                                                                      Focusable="false"
                                                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      ClickMode="Press"
                                                                      Background="Transparent"
                                                                      BorderThickness="0">
                                                            <Path x:Name="Arrow"
                                                                  Fill="White"
                                                                  HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                        </ToggleButton>
                                                        <Popup x:Name="Popup"
                                                               Placement="Bottom"
                                                               IsOpen="{TemplateBinding IsDropDownOpen}"
                                                               AllowsTransparency="True"
                                                               Focusable="False"
                                                               PopupAnimation="Slide">
                                                            <Grid x:Name="DropDown"
                                                                  SnapsToDevicePixels="True"
                                                                  MinWidth="{TemplateBinding ActualWidth}"
                                                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                                                <Border x:Name="DropDownBorder"
                                                                        Background="Black"
                                                                        BorderBrush="#555"
                                                                        BorderThickness="1">
                                                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                                    </ScrollViewer>
                                                                </Border>
                                                            </Grid>
                                                        </Popup>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ComboBox.Style>
                            </ComboBox>
                        </StackPanel>

                        <Button Margin="5" HorizontalAlignment="Stretch" Height="50" Command="{Binding CreateCommand}">Создать</Button>
                        <Button Margin="5" HorizontalAlignment="Stretch" Height="50" Command="{Binding CloseCommand}">Отмена</Button>
                    </StackPanel>
                </Border>
            </componentsViews:Container>
        </Border>
    </Grid>
</UserControl>
