# ChangePasswordView - Модальное окно смены пароля

## Обзор
Отдельное модальное окно для смены пароля пользователя с современным дизайном.

## Файлы
- **View**: `Views/Modals/ChangePasswordView.xaml`
- **Code-behind**: `Views/Modals/ChangePasswordView.xaml.cs`
- **ViewModel**: `ViewModels/ModalsViewModels/ChangePasswordViewModel.cs`

## Функциональность

### Основные возможности
1. **Валидация полей**
   - Проверка заполненности всех полей
   - Проверка совпадения нового пароля и подтверждения
   - Визуальные индикаторы ошибок (⚠)

2. **Проверка безопасности**
   - Проверка текущего пароля
   - Защита от изменения пароля root-профиля
   - Минимальная длина пароля (рекомендация 6+ символов)

3. **Обновление данных**
   - Сохранение нового пароля в базу данных
   - Автоматическое закрытие окна после успешной смены
   - Уведомления об успехе/ошибках через CustomMessageBox

## Дизайн

### Цветовая схема
- **Заголовок**: Фиолетовый градиент (#6366f1 → #8b5cf6)
- **Фон**: Очень тёмный (#0d0d0d)
- **Карточки**: Чёрный (#0a0a0a)
- **Кнопка "Сохранить"**: Зелёный градиент (#10b981 → #059669)
- **Кнопка "Отмена"**: Тёмно-серый градиент

### UI элементы
- **Иконка**: 🔐 (замок с ключом)
- **Информационный блок**: Подсказки о требованиях к паролю
- **Скруглённые углы**: 20px (основной контейнер), 12px (карточки), 8px (кнопки)
- **Тени**: DropShadowEffect для глубины
- **Анимации**: Эффекты при наведении и нажатии кнопок

## Использование

### Открытие из AccountManagementViewModel
```csharp
private async Task OpenChangePasswordModal()
{
    await ModalService.Instance.ShowAsync<ChangePasswordView, ChangePasswordViewModel>(
        modalId: null,
        options: null,
        showOverlay: true,
        closeOnBackgroundClick: true
    );
}
```

### Регистрация в Bootstrapper.cs
```csharp
containerRegistry.RegisterForNavigation<ChangePasswordView, ChangePasswordViewModel>();
```

## Зависимости
- **AuthorizationService**: Получение текущего пользователя, проверка root-профиля
- **UserService**: Обновление пароля в базе данных
- **CustomMessageBox**: Отображение уведомлений
- **ModalService**: Управление модальными окнами

## Валидация

### Проверки перед сменой пароля
1. Все поля заполнены
2. Новый пароль совпадает с подтверждением
3. Пользователь не является root-профилем
4. Текущий пароль введён корректно

### Визуальная обратная связь
- Красные предупреждения под полями при ошибках
- Автоматическое скрытие предупреждений при вводе корректных данных
- Toast-уведомления об успехе/ошибках

## Связь с AccountManagementView
AccountManagementView теперь упрощён:
- Убрана раскрывающаяся форма смены пароля
- Добавлена кнопка "🔐 Сменить пароль" с фиолетовым градиентом
- Клик по кнопке открывает отдельное модальное окно ChangePasswordView

## Преимущества разделения
1. **Лучшая организация кода**: Логика смены пароля изолирована
2. **Упрощённый UI**: AccountManagement стал компактнее и понятнее
3. **Переиспользуемость**: ChangePasswordView можно открыть из любого места
4. **Лучший UX**: Отдельное окно фокусирует внимание на задаче смены пароля
