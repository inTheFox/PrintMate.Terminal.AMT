<UserControl x:Class="PrintMate.Terminal.Views.Modals.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:componentsViews="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d"
             Width="1920" Height="1080"
             d:DesignHeight="1080" d:DesignWidth="1920">

    <UserControl.Resources>
        <!-- Стиль для кнопок -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Главный контейнер -->
        <Border Width="550"
                CornerRadius="20"
                Background="#0d0d0d"
                BorderThickness="1"
                Padding="15"
                BorderBrush="#2a2a2a">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.7"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Заголовок с градиентом -->
                <Border Grid.Row="0" CornerRadius="20,20,0,0" Height="80">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#6366f1" Offset="0"/>
                            <GradientStop Color="#8b5cf6" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Grid Margin="30,0">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="🔐 Смена пароля"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                            <TextBlock Text="Обновите пароль для вашей учетной записи"
                                       FontSize="13"
                                       Foreground="#f0f0f0"
                                       Opacity="0.9"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Основное содержимое -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              Padding="30,30,30,30">
                    <StackPanel>
                        <!-- Информационное сообщение -->
                        <Border Background="#1a1a2e"
                                CornerRadius="10"
                                Padding="15"
                                Margin="0,10,0,25"
                                BorderThickness="1"
                                BorderBrush="#2a2a4e">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ℹ️"
                                           FontSize="20"
                                           VerticalAlignment="Top"
                                           Margin="0,0,10,0"/>
                                <TextBlock TextWrapping="Wrap"
                                           Foreground="#b3b3ff"
                                           FontSize="13"
                                           LineHeight="20">
                                    <Run Text="Убедитесь, что новый пароль содержит не менее 6 символов."/>
                                    <LineBreak/>
                                    <Run Text="После смены пароля вы останетесь авторизованы в системе."/>
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Форма смены пароля -->
                        <Border Background="#0a0a0a"
                                CornerRadius="12"
                                Padding="25"
                                BorderThickness="1"
                                BorderBrush="#1a1a1a">
                            <StackPanel>
                                <!-- Текущий пароль -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Текущий пароль"
                                               Foreground="#999"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Visibility="{Binding OldPasswordDangerVisibility}"
                                               Text="⚠ Это обязательное поле"
                                               HorizontalAlignment="Left"
                                               Foreground="#ff4444"
                                               FontSize="12"
                                               Margin="0,0,0,5"/>
                                    <componentsViews:EditboxKeyboard Label="Введите текущий пароль"
                                                                     Value="{Binding OldPassword, Mode=TwoWay}"/>
                                </StackPanel>

                                <!-- Новый пароль -->
                                <StackPanel Margin="0,0,0,20">
                                    <TextBlock Text="Новый пароль"
                                               Foreground="#999"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Visibility="{Binding NewPasswordDangerVisibility}"
                                               Text="⚠ Это обязательное поле"
                                               HorizontalAlignment="Left"
                                               Foreground="#ff4444"
                                               FontSize="12"
                                               Margin="0,0,0,5"/>
                                    <componentsViews:EditboxKeyboard Label="Введите новый пароль"
                                                                     Value="{Binding NewPassword, Mode=TwoWay}"/>
                                </StackPanel>

                                <!-- Подтверждение пароля -->
                                <StackPanel>
                                    <TextBlock Text="Подтверждение пароля"
                                               Foreground="#999"
                                               FontSize="13"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Visibility="{Binding ConfirmPasswordDangerVisibility}"
                                               Text="⚠ Пароли не совпадают"
                                               HorizontalAlignment="Left"
                                               Foreground="#ff4444"
                                               FontSize="12"
                                               Margin="0,0,0,5"/>
                                    <componentsViews:EditboxKeyboard Label="Повторите новый пароль"
                                                                     Value="{Binding ConfirmPassword, Mode=TwoWay}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Кнопки действий -->
                        <Grid Margin="0,25,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Кнопка сохранения -->
                            <Button Grid.Column="0"
                                    Height="55"
                                    Command="{Binding ChangePasswordCommand}"
                                    Style="{StaticResource ModernButton}">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#10b981" Offset="0"/>
                                        <GradientStop Color="#059669" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✓ Сохранить" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- Кнопка отмены -->
                            <Button Grid.Column="2"
                                    Height="55"
                                    Command="{Binding CancelCommand}"
                                    Style="{StaticResource ModernButton}">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#2a2a2a" Offset="0"/>
                                        <GradientStop Color="#1f1f1f" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✕ Отмена" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
