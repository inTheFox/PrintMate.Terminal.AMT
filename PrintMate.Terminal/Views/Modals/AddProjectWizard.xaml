<UserControl x:Class="PrintMate.Terminal.Views.Modals.AddProjectWizard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PrintMate.Terminal.Views.Modals"
             Width="1920" Height="1080"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <!-- Стиль анимации появления -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="0.95" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="0.95" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Основной контейнер -->
        <Border x:Name="MainBorder"
                ClipToBounds="True"
                Width="900" Height="800"
                Background="#121212"
                CornerRadius="12"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="Black"/>
            </Border.Effect>

            <Grid>
                <!-- Контейнер для страниц с анимацией переходов -->
                <Grid x:Name="ContentHost" ClipToBounds="True">
                    <!-- Текущая страница -->
                    <ContentPresenter x:Name="CurrentPage" />
                    <!-- Следующая страница (для анимации) -->
                    <ContentPresenter x:Name="NextPage" Visibility="Collapsed" />
                </Grid>

                <!-- Индикатор шагов внизу -->
                <StackPanel Orientation="Horizontal" Opacity="0"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Bottom"
                           Margin="0,0,0,20">
                    <Ellipse x:Name="Step1Indicator" Width="8" Height="8" Fill="#666" Margin="4,0"/>
                    <Ellipse x:Name="Step2Indicator" Width="8" Height="8" Fill="#333" Margin="4,0"/>
                    <Ellipse x:Name="Step3Indicator" Width="8" Height="8" Fill="#333" Margin="4,0"/>
                    <Ellipse x:Name="Step4Indicator" Width="8" Height="8" Fill="#333" Margin="4,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
