<UserControl x:Class="PrintMate.Terminal.Views.Modals.AccountManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="1024" Height="768"
             d:DesignHeight="768" d:DesignWidth="1024">

    <UserControl.Resources>
        <!-- Стиль для кнопок -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для текстовых полей (только чтение) -->
        <Style x:Key="ProfileTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <!-- Главный контейнер -->
        <Border Width="600"
                CornerRadius="20"
                Background="#0d0d0d"
                BorderThickness="1"
                Padding="15"
                BorderBrush="#2a2a2a">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.7"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Заголовок с градиентом -->
                <Border Grid.Row="0" CornerRadius="20,20,0,0" Height="80">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#ff6f00" Offset="0"/>
                            <GradientStop Color="#ff8c1a" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Grid Margin="30,0">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Управление учетной записью"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                            <TextBlock Text="Информация профиля и настройки"
                                       FontSize="13"
                                       Foreground="#f0f0f0"
                                       Opacity="0.9"
                                       Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Основное содержимое -->
                <ScrollViewer Grid.Row="1"
                              VerticalScrollBarVisibility="Auto"
                              Padding="30,20,30,30">
                    <StackPanel>
                        <!-- Профиль пользователя -->
                        <Border Background="#0a0a0a"
                                CornerRadius="12"
                                Padding="25"
                                Margin="0,0,0,20"
                                BorderThickness="1"
                                BorderBrush="#1a1a1a">
                            <StackPanel>
                                <TextBlock Text="📋 Информация профиля"
                                           Foreground="#ff6f00"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Margin="0,0,0,20"/>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Фамилия"
                                               Foreground="#999"
                                               FontSize="12"
                                               Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding Family, Mode=OneWay}"
                                             Style="{StaticResource ProfileTextBox}"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,15">
                                    <TextBlock Text="Имя"
                                               Foreground="#999"
                                               FontSize="12"
                                               Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding Name, Mode=OneWay}"
                                             Style="{StaticResource ProfileTextBox}"/>
                                </StackPanel>

                                <StackPanel>
                                    <TextBlock Text="Логин"
                                               Foreground="#999"
                                               FontSize="12"
                                               Margin="0,0,0,8"/>
                                    <TextBox Text="{Binding Login, Mode=OneWay}"
                                             Style="{StaticResource ProfileTextBox}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Секция безопасности -->
                        <Border Background="#0a0a0a"
                                CornerRadius="12"
                                Padding="25"
                                Margin="0,0,0,20"
                                BorderThickness="1"
                                BorderBrush="#1a1a1a">
                            <StackPanel>
                                <TextBlock Text="🔒 Безопасность"
                                           Foreground="#ff6f00"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Margin="0,0,0,15"/>

                                <!-- Кнопка смены пароля -->
                                <Button HorizontalAlignment="Stretch"
                                        Height="55"
                                        Command="{Binding ChangePasswordCommand}"
                                        Style="{StaticResource ModernButton}">
                                    <Button.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#6366f1" Offset="0"/>
                                            <GradientStop Color="#8b5cf6" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="🔐 Сменить пароль" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Действия -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Кнопка выхода -->
                            <Button Grid.Column="0"
                                    Height="55"
                                    Command="{Binding LogoutCommand}"
                                    Style="{StaticResource ModernButton}">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#dc3545" Offset="0"/>
                                        <GradientStop Color="#c82333" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="⎋ Выйти" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <!-- Кнопка закрытия -->
                            <Button Grid.Column="2"
                                    Height="55"
                                    Command="{Binding CloseFormCommand}"
                                    Style="{StaticResource ModernButton}">
                                <Button.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#2a2a2a" Offset="0"/>
                                        <GradientStop Color="#1f1f1f" Offset="1"/>
                                    </LinearGradientBrush>
                                </Button.Background>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✕ Закрыть" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
