<UserControl x:Class="PrintMate.Terminal.Views.ProjectPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views"
             xmlns:componentsViews="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="724">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#0b0b0b" Padding="30 20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="40">Печать текущего проекта</TextBlock>
                <TextBlock Margin="0 5 0 0" HorizontalAlignment="Center" Foreground="Gray" FontWeight="Medium" FontSize="14">менеджер проектов</TextBlock>
            </StackPanel>
        </Border>

        <!-- 2D визуализация слоя (LayerCanvas на весь экран) -->
        <local:LayerCanvas x:Name="LayerCanvas"
                          Grid.Row="1"/>
    </Grid>
</UserControl>
