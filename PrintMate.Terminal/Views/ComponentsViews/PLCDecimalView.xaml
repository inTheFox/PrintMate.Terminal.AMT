<UserControl x:Class="PrintMate.Terminal.Views.ComponentsViews.PLCDecimalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d" 
             >

    <Border Background="#161616" Padding="20" CornerRadius="5" Margin="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>


            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Grid.Column="0" FontSize="15" x:Name="TextBlock" TextAlignment="Left" HorizontalAlignment="Left" TextWrapping="Wrap" Foreground="White" Text="{Binding CommandInfo.Title, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <TextBlock FontSize="15" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="IndicatorText" FontWeight="Bold" Foreground="White" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            
            <Border Margin="15 0 0 0" Grid.Column="2 " Background="Black" CornerRadius="5" Padding="5">
                <Image x:Name="Image" Width="30" Grid.Column="2" />
            </Border>
        </Grid>
    </Border>
</UserControl>
