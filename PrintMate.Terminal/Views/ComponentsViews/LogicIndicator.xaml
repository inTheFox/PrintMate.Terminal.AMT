<UserControl x:Class="PrintMate.Terminal.Views.ComponentsViews.LogicIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:componentsViews="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d">
    <Border Background="#161616" Padding="20" CornerRadius="5" Margin="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Grid.Column="0" FontSize="15" x:Name="TextBlock"
                       TextAlignment="Left" HorizontalAlignment="Left" TextWrapping="Wrap"
                       Foreground="White"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type componentsViews:LogicIndicator}}}"/>
            <Image VerticalAlignment="Center" Width="10" x:Name="Image" Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>