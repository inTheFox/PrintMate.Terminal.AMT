<UserControl x:Class="PrintMate.Terminal.Views.ComponentsViews.EditboxKeyboard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrintMate.Terminal.Views.ComponentsViews"
             mc:Ignorable="d">
    <Grid Margin="1">
        <!--<Border Padding="5" Background="#171717" CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Border Background="Black" Padding="5" CornerRadius="10">
                    <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" FontSize="15" Foreground="White" FontWeight="Bold" Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Border>
                <Border Margin="5 0 0 0" Grid.Column="1" Background="#1b1b1b" Padding="5" CornerRadius="10">
                    <TextBlock Margin="10" FontSize="15" Foreground="White" FontWeight="Medium" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </Border>
            </Grid>
        </Border>-->
                


            <StackPanel>
                <TextBlock Margin="10" FontSize="15" Foreground="White" FontWeight="Medium" Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <Border BorderBrush="#313131" BorderThickness="2" Background="#161616" CornerRadius="5" Padding="5" MouseDown="UIElement_OnMouseDown">
                    <Border Background="Transparent" Padding="10" CornerRadius="0">
                        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="15" Foreground="White" FontWeight="Medium" Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </Border>
                </Border>
            </StackPanel>
      
    </Grid>
</UserControl>
