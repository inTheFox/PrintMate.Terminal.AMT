# Сравнение алгоритмов расчета диаметра пучка

## 📊 Таблица сравнения

| Аспект | Старый алгоритм (с интерполяцией) | Новый алгоритм (Java порт) |
|--------|-----------------------------------|----------------------------|
| **Источник** | Эксперимент + теория | Точный порт Java кода |
| **Интерполяция** | ✅ Линейная интерполяция между точками | ❌ Нет интерполяции |
| **Диаметр** | Вычисляется для каждой точки | Устанавливается один раз для региона |
| **Z координата** | Рассчитывается через BeamDiameterCalibration | Рассчитывается через getLensTravelMicron |
| **Зависимость от (x,y)** | ❌ Нет | ✅ Да, через getCurrentFocalLengthMm |
| **Формула** | Табличные данные + интерполяция | Гауссов пучок: zR * sqrt((d/d₀)² - 1) |

## 🔴 Старый алгоритм (НЕПРАВИЛЬНЫЙ)

### Код (упрощенно)
```csharp
// 1. Для каждого региона вычисляли Z offset один раз
double targetDiameter = region.Parameters.LaserBeamDiameter;
float zOffset = BeamDiameterCalibration.CalculateZForDiameter(targetDiameter);

// 2. Затем применяли этот offset ко ВСЕМ точкам
for (int i = 0; i < polyLine.Points.Count; i++)
{
    var point = polyLine.Points[i];

    hansPoints[i] = new structUdmPos
    {
        x = point.X,
        y = point.Y,
        z = zOffset,  // ❌ Одинаковая Z для всех точек!
        a = 0
    };
}
```

### Проблемы
1. ❌ **Игнорирует координаты (x, y)** - Z одинаковая для всех точек региона
2. ❌ **Использует табличные данные** - Не учитывает реальные параметры луча
3. ❌ **Линейная интерполяция** - Между точками региона с разными диаметрами
4. ❌ **Не соответствует Java коду** - Использует совершенно другую логику

### Пример ошибки
```
Регион с диаметром 100 мкм:
  Точка 1: (0, 0) -> Z = 0.002739 мм  ✓
  Точка 2: (50, 0) -> Z = 0.002739 мм  ❌ Должна быть другая!
  Точка 3: (100, 0) -> Z = 0.002739 мм  ❌ Должна быть другая!

ПРОБЛЕМА: Точки на разном расстоянии от центра имеют одинаковую Z!
```

## 🟢 Новый алгоритм (ПРАВИЛЬНЫЙ - Java порт)

### Код (точный порт)
```csharp
// 1. Устанавливаем текущий диаметр (DIAMETER operation в Java)
_currentBeamDiameterMicron = region.Parameters.LaserBeamDiameter;

// 2. Для КАЖДОЙ точки вычисляем Z с учетом ее координат
for (int i = 0; i < polyLine.Points.Count; i++)
{
    var point = polyLine.Points[i];

    // ✅ Учитываем координаты точки!
    double correctedZ = GetCorrectZValue(point.X, point.Y, 0.0f);

    hansPoints[i] = new structUdmPos
    {
        x = point.X,
        y = point.Y,
        z = (float)correctedZ,  // ✅ Разная Z для каждой точки!
        a = 0
    };
}
```

### GetCorrectZValue (Java BeamConfig.getCorrectZValue)
```csharp
double GetCorrectZValue(float x, float y, float z)
{
    // Шаг 1: Вычисляем фокусное расстояние от точки до линзы
    // ✅ Зависит от (x, y)!
    double focalLengthMm = sqrt(x² + y² + (baseFocal + z)²);
    double focalLengthMicron = focalLengthMm * 1000;

    // Шаг 2: Добавляем смещение от диаметра
    // ✅ Использует формулу гауссова пучка!
    if (EnableDiameterChange) {
        double lensTravelMicron = rayleighLength * sqrt((d/d₀)² - 1);
        focalLengthMicron += lensTravelMicron;
    }

    // Шаг 3: Вычитаем power offset
    if (EnablePowerOffset && power > maxPower * 0.15) {
        double powerOffset = GetPowerOffset(power);
        focalLengthMicron -= powerOffset;
    }

    // Шаг 4: Преобразуем через полином
    if (EnableZCorrection) {
        double f = focalLengthMicron / 1000;
        return a*f² + b*f + c;
    }

    return z;  // Для SLM всегда 0
}
```

### Преимущества
1. ✅ **Учитывает координаты (x, y)** - Каждая точка имеет правильную Z
2. ✅ **Формула гауссова пучка** - Физически корректный расчет
3. ✅ **Соответствует Java коду** - Точный порт, строка в строку
4. ✅ **Нет интерполяции** - Диаметр устанавливается один раз, как в Java

### Пример правильного расчета
```
Регион с диаметром 100 мкм:
  Точка 1: (0, 0) -> focalLength = 538.46 mm -> Z = 0.002739 мм  ✓
  Точка 2: (50, 0) -> focalLength = 540.78 mm -> Z = 0.002771 мм  ✓
  Точка 3: (100, 0) -> focalLength = 547.68 mm -> Z = 0.002867 мм  ✓

ПРАВИЛЬНО: Z зависит от расстояния точки от центра!
```

## 📐 Физический смысл

### Почему Z должна зависеть от (x, y)?

Представим линзу на высоте 538.46 мм над рабочей плоскостью:

```
        Линза (F-theta объектив)
             ●
            /|\
           / | \
          /  |  \
         /   |   \
        /    |    \
       /     |     \
      /      |      \
     /       |       \
    ●--------●--------●  Рабочая плоскость (Z=0)
   (100,0)  (0,0)   (50,0)

Расстояния от линзы до точек:
  (0, 0):    d = sqrt(0² + 0² + 538.46²) = 538.46 мм  (минимум!)
  (50, 0):   d = sqrt(50² + 0² + 538.46²) = 540.78 мм
  (100, 0):  d = sqrt(100² + 0² + 538.46²) = 547.68 мм
```

**Важно:** Расстояние от линзы до точки влияет на фокусировку!
- Чем дальше от центра → больше расстояние → другая Z для того же диаметра

### Формула гауссова пучка

```
Диаметр луча в зависимости от Z:

d(z) = d₀ * sqrt(1 + (z/zR)²)

где:
  d₀ - минимальный диаметр (в фокусе)
  z - расстояние от фокуса
  zR - длина Рэлея (Rayleigh length)
```

**Обратная формула** (используется в Java):
```
z = zR * sqrt((d/d₀)² - 1)
```

Для вашей конфигурации:
- d₀ = 63.1 мкм
- zR = 1863.0 мкм

Пример:
```
Целевой диаметр 100 мкм:
z = 1863 * sqrt((100/63.1)² - 1)
z = 1863 * sqrt(2.5119 - 1)
z = 1863 * sqrt(1.5119)
z = 1863 * 1.2296
z = 2290.7 мкм = 2.291 мм

Но! Это z от фокуса, а нам нужна Z координата для UDM.
Поэтому добавляем к focalLength и преобразуем через полином.
```

## 🔬 Сравнение результатов

### Тест: Диаметр 100 мкм, точки на разных расстояниях

| Точка | Координаты | Старый алгоритм | Новый алгоритм | Разница |
|-------|-----------|----------------|----------------|---------|
| 1 | (0, 0) | Z = 0.002739 | Z = 0.002739 | 0.000 мм |
| 2 | (50, 0) | Z = 0.002739 | Z = 0.002771 | **0.032 мм** |
| 3 | (100, 0) | Z = 0.002739 | Z = 0.002867 | **0.128 мм** |
| 4 | (100, 100) | Z = 0.002739 | Z = 0.002995 | **0.256 мм** |

**Вывод:** Старый алгоритм давал ошибку до 0.25 мм для точек на краю поля!

### Влияние на диаметр пятна

Ошибка 0.25 мм в Z → изменение диаметра:

```
Исходный диаметр: 100 мкм
При Z + 0.25 мм:
  d = 63.1 * sqrt(1 + ((2.291 + 0.25) / 1.863)²)
  d = 63.1 * sqrt(1 + (1.364)²)
  d = 63.1 * sqrt(1 + 1.860)
  d = 63.1 * 1.691
  d = 106.7 мкм

ОШИБКА: 6.7 мкм (6.7%!)
```

## ✅ Вывод

### Старый алгоритм
- ❌ Игнорировал влияние координат (x, y)
- ❌ Давал ошибку до 7% на краях поля
- ❌ Не соответствовал Java коду
- ❌ Использовал экспериментальные данные вместо физической модели

### Новый алгоритм (Java порт)
- ✅ Учитывает координаты (x, y)
- ✅ Использует физически корректную формулу
- ✅ Точно соответствует Java коду
- ✅ Проверяемый и предсказуемый результат

## 🎯 Рекомендации

1. **Использовать новый алгоритм** для всех расчетов
2. **Протестировать на реальном оборудовании** - замерить диаметры пятен
3. **Сравнить результаты** старого и нового алгоритма
4. **Обновить ProductionUdmBuilder** с новой логикой

---

**Важно:** Новый алгоритм - это НЕ эксперимент, а точный порт проверенного Java кода!
