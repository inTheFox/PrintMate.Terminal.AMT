# ✅ Текущий статус калибровки

## 🎯 Прогресс

**ХОРОШАЯ НОВОСТЬ:** Z координата РАБОТАЕТ! Коэффициент K_FACTOR_AXES_Z = 4 был применен.

### Результат теста:
- **Запрошено:** 80 мкм
- **Получено:** 61 мкм
- **Ошибка:** -19 мкм (-23.75%)

Это означает, что диаметр **меньше** запрошенного на ~24%.

## 📊 Следующие шаги

### 1. Запустить калибровочный анализ

```csharp
RunDiameterTests.QuickCalibration();
```

Это покажет рекомендуемую коррекцию параметров на основе измерения (80 → 61).

### 2. Прожечь дополнительные точки

Для точной калибровки нужно **3-4 измерения** с разными диаметрами:

```csharp
// Тест 1
RunDiameterTests.RunQuick(100.0, 200f);  // Запрос: 100 мкм

// Тест 2
RunDiameterTests.RunQuick(150.0, 200f);  // Запрос: 150 мкм

// Тест 3
RunDiameterTests.RunQuick(200.0, 200f);  // Запрос: 200 мкм
```

**Прожгите эти точки и измерьте диаметры!**

### 3. Ввести все измерения в калибровочный инструмент

```csharp
using HansDebuggerApp.Hans;
using System.Collections.Generic;

var config = RunDiameterTests.LoadConfiguration();

var measurements = new List<CalibrationHelper.Measurement>
{
    new CalibrationHelper.Measurement { RequestedDiameterMicron = 80.0, MeasuredDiameterMicron = 61.0 },
    new CalibrationHelper.Measurement { RequestedDiameterMicron = 100.0, MeasuredDiameterMicron = ??? },  // Ваше измерение
    new CalibrationHelper.Measurement { RequestedDiameterMicron = 150.0, MeasuredDiameterMicron = ??? },  // Ваше измерение
    new CalibrationHelper.Measurement { RequestedDiameterMicron = 200.0, MeasuredDiameterMicron = ??? }   // Ваше измерение
};

CalibrationHelper.AnalyzeAndSuggestCorrection(config, measurements);
```

### 4. Обновить конфигурацию

Инструмент выдаст **новые рекомендуемые параметры** в формате JSON. Скопируйте их в `scanator_config_test.json`.

### 5. Повторить тест для проверки

После обновления конфигурации запустите повторный тест:

```csharp
RunDiameterTests.RunQuick(100.0, 200f);
```

Прожгите и измерьте. Если ошибка < 5% - калибровка завершена!

## 🔍 Предварительный анализ (80 → 61)

Коэффициент: 61/80 = 0.7625

Это означает что диаметры на **~24% меньше** запрошенных.

### Вариант 1: Увеличить Rayleigh Length

```
Текущий rayleighLengthMicron: 1863.0
Рекомендуемый: 1863.0 / 0.7625 = 2443 мкм
```

### Вариант 2: Уменьшить minBeamDiameter

```
Текущий minBeamDiameterMicron: 63.1
Рекомендуемый: 63.1 * 0.7625 = 48.1 мкм
```

**Рекомендация:** Используйте Вариант 1 (увеличить Rayleigh Length), так как minBeamDiameter обычно измеряется физически и должен быть точным.

## ⚠️ ВАЖНО

Для точной калибровки нужно **минимум 3-4 измерения** с разными диаметрами!

Одно измерение (80 → 61) дает только грубую оценку. Прожгите дополнительные точки для повышения точности.

## 📝 Что было исправлено

1. ✅ **K_FACTOR_AXES_Z = 4** применен к `UDM_AddPoint2D`
   - Z координата теперь правильно масштабируется
   - Проблема "все диаметры ≈50 мкм" решена

2. ✅ **Создан CalibrationHelper** для автоматического расчета коррекции
   - Анализирует измерения
   - Предлагает новые параметры
   - Выводит готовую JSON конфигурацию

3. ✅ **Добавлен метод RunDiameterTests.QuickCalibration()**
   - Быстрый доступ к калибровке

## 🚀 Краткая инструкция

1. Запустите `RunDiameterTests.QuickCalibration()` - увидите предварительный анализ
2. Прожгите 3 дополнительные точки (100, 150, 200 мкм)
3. Измерьте диаметры
4. Введите все 4 измерения в `CalibrationHelper.AnalyzeAndSuggestCorrection()`
5. Скопируйте новую конфигурацию в JSON
6. Запустите повторный тест для проверки

---

**Дата:** 2025-01-XX
**Статус:** ✅ Z координата работает, требуется калибровка параметров
**Следующий шаг:** Прожечь дополнительные точки и измерить диаметры
