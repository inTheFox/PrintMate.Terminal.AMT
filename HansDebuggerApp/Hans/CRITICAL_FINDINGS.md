# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Сканер не применяет Z координату

## 📊 Реальные измерения

| Запрошенный диаметр (мкм) | Измеренный диаметр (мкм) | Ошибка (мкм) | Ошибка (%) |
|---------------------------|--------------------------|--------------|------------|
| 65                        | 49.6                     | **-15.4**    | **-23.7%** |
| 85                        | 50.3                     | **-34.7**    | **-40.8%** |
| 95                        | 56.0                     | **-39.0**    | **-41.1%** |
| 500                       | 122.0                    | **-378.0**   | **-75.6%** |

## 🔍 Анализ проблемы

### Ключевое наблюдение
**ВСЕ измеренные диаметры ≈ 50-56 мкм**, что близко к минимальному диаметру конфигурации:
- `minBeamDiameterMicron = 63.1 мкм`
- Измеренные: 49.6, 50.3, 56.0, 122.0 мкм

### Вывод
**Сканер работает В ФОКУСЕ независимо от заданной Z координаты!**

Это означает, что:
- ❌ Z координата НЕ применяется к управлению фокусом луча
- ❌ Расфокусировка НЕ работает
- ❌ Контроль диаметра пучка НЕ работает

## 🔧 Возможные причины

### 1. 3D коррекция отключена в Hans сканере

**Проверка:**
```csharp
// В TestUdmBuilder.cs закомментированы строки:
// double[] paraK = _config.ThirdAxisConfig.CorrectionPolynomial;
// int correctionResult = UDM_Set3dCorrectionPara(
//     (float)_config.BeamConfig.FocalLengthMm,
//     paraK,
//     paraK.Length
// );
```

**Решение:**
Попробуйте РАСКОММЕНТИРОВАТЬ эти строки и передать полином коррекции в SDK.

---

### 2. Полином Z-коррекции неправильный

**Текущий полином:**
```json
{
  "afactor": 0.0,
  "bfactor": 0.013879796,
  "cfactor": -7.4747047
}
```

Это линейный полином: `Z = 0.01388*f - 7.4747`

**Проверка:** При f = 538.46 мм (фокус):
```
Z = 0.01388 * 538.46 - 7.4747
Z = 7.474 - 7.4747
Z ≈ -0.0007 мм ← очень близко к нулю ✓
```

Полином выглядит правильным для базового фокуса.

**Но!** Когда мы добавляем смещение линзы, Z координата становится слишком маленькой.

Пример для d=100 мкм:
```
lensTravelMicron = 1470 мкм
focalLength = 538.46 + 1.47 = 539.93 мм
Z = 0.01388 * 539.93 - 7.4747
Z = 7.4944 - 7.4747
Z = 0.0197 мм ← ОЧЕНЬ МАЛО!
```

**Проблема:** Z координата в диапазоне 0.001-0.02 мм может быть слишком маленькой для сканера.

---

### 3. SDK Hans не передает Z в UDM_AddPoint2D

**Текущий код:**
```csharp
structUdmPos point = new structUdmPos
{
    x = x,
    y = y,
    z = zCoord,  // ← Передается ли это в сканер?
    a = 0
};

UDM_AddPoint2D(point, dwellTimeMs, 0);
```

**Возможно:** SDK игнорирует поле `z` в `UDM_AddPoint2D` и использует только `UDM_AddPolyline3D`.

---

### 4. Минимальный диаметр в конфиге неправильный

**Конфиг:**
```json
"minBeamDiameterMicron": 63.1
```

**Измерения:** 49.6, 50.3, 56 мкм

Реальный минимальный диаметр ≈ 50 мкм, а не 63.1 мкм!

**Это может означать:**
- Неправильно измерен минимальный диаметр
- ИЛИ Rayleigh Length неправильный
- ИЛИ M² неправильный

---

## ✅ ЧТО ДЕЛАТЬ (пошагово)

### Шаг 1: Запустить диагностический тест

```csharp
using HansDebuggerApp.Hans;

// Анализирует ваши реальные измерения
RunDiameterTests.AnalyzeRealMeasurements();
```

Этот тест выведет подробный анализ каждого измерения и покажет где проблема.

---

### Шаг 2: Проверить что Z координата передается в сканер

**Изменить TestUdmBuilder.cs:**

Вместо `UDM_AddPoint2D` попробовать `UDM_AddPolyline3D`:

```csharp
// БЫЛО:
UDM_AddPoint2D(point, dwellTimeMs, 0);

// ПОПРОБУЙТЕ:
structUdmPos[] polyline = new structUdmPos[] { point };
UDM_AddPolyline3D(polyline, 1, 0);
```

**Причина:** `UDM_AddPolyline3D` может правильно обрабатывать Z координату, а `UDM_AddPoint2D` - игнорировать.

---

### Шаг 3: Включить 3D коррекцию в SDK

**В TestUdmBuilder.cs найти строки 442-458:**

```csharp
// РАСКОММЕНТИРОВАТЬ:
double[] paraK = _config.ThirdAxisConfig.CorrectionPolynomial;
int correctionResult = UDM_Set3dCorrectionPara(
    (float)_config.BeamConfig.FocalLengthMm,
    paraK,
    paraK.Length
);

if (correctionResult != 0)
{
    Console.WriteLine($"⚠️ Warning: 3D correction setup returned code {correctionResult}");
}
else
{
    Console.WriteLine($"✓ 3D correction enabled: focalLength={_config.BeamConfig.FocalLengthMm}mm, polynomial length={paraK.Length}");
}
```

**Но!** Нужно добавить `CorrectionPolynomial` в конфиг:

```json
"thirdAxisConfig": {
  "afactor": 0.0,
  "bfactor": 0.013879796,
  "cfactor": -7.4747047,
  "correctionPolynomial": [-7.4747047, 0.013879796, 0.0]
}
```

---

### Шаг 4: Увеличить Z координату (экспериментально)

Попробуйте УМНОЖИТЬ Z на коэффициент:

```csharp
// В GetCorrectZValue, после расчета zFinal:
zFinal = zFinal * 10;  // Или другой коэффициент

Console.WriteLine($"   Z final (с коэффициентом x10): {zFinal:F6} мм");
```

Если после этого диаметр изменится - проблема в масштабе Z координаты.

---

### Шаг 5: Проверить реальный минимальный диаметр

**Прожечь точку с Z = 0:**

```csharp
builder.BuildSinglePoint(
    x: 0f,
    y: 0f,
    z: 0f,  // Ручная Z = 0
    beamDiameterMicron: 63.1,
    powerWatts: 200f,
    dwellTimeMs: 500
);
```

**Измерить диаметр** этой точки - это и будет реальный `minBeamDiameterMicron`.

Затем обновить конфиг:
```json
"minBeamDiameterMicron": <измеренное значение>
```

И пересчитать Rayleigh Length:
```
zR = π * (d₀/2)² / (λ * M²)
```

---

### Шаг 6: Проверить работу Z на простом тесте

**Создать 3 точки с разными Z вручную:**

```csharp
// Z = 0 (фокус)
builder.BuildSinglePoint(0f, 0f, 0f, 63.1, 200f, 500);

// Z = 0.05 мм (небольшая расфокусировка)
builder.BuildSinglePoint(0f, 0f, 0.05f, 63.1, 200f, 500);

// Z = 0.1 мм (большая расфокусировка)
builder.BuildSinglePoint(0f, 0f, 0.1f, 63.1, 200f, 500);
```

**Прожечь эти точки и измерить диаметры.**

Если диаметры одинаковые - проблема в SDK или настройках сканера.
Если диаметры разные - проблема в расчете Z координаты.

---

## 📝 Чеклист диагностики

- [ ] Запустить `RunDiameterTests.AnalyzeRealMeasurements()`
- [ ] Попробовать `UDM_AddPolyline3D` вместо `UDM_AddPoint2D`
- [ ] Включить 3D коррекцию через `UDM_Set3dCorrectionPara`
- [ ] Измерить реальный минимальный диаметр (прожечь точку с Z=0)
- [ ] Проверить работу Z на тесте с ручными Z координатами (0, 0.05, 0.1)
- [ ] Попробовать умножить Z на коэффициент (x10, x100)
- [ ] Проверить настройки Hans сканера (3D режим включен?)
- [ ] Проверить калибровку полинома в Hans Laser Marker

---

## 🎯 Наиболее вероятная причина

**Сканер не применяет Z координату потому что:**

1. **UDM_AddPoint2D игнорирует Z** - используйте `UDM_AddPolyline3D`
2. **ИЛИ 3D коррекция не активирована** - вызовите `UDM_Set3dCorrectionPara`
3. **ИЛИ Hans сканер в 2D режиме** - проверьте настройки оборудования

---

## 💡 Рекомендация

**НАЧНИТЕ С ПРОСТОГО:**

1. Прожгите точку с Z=0.1 мм вручную:
   ```csharp
   builder.BuildSinglePoint(0f, 0f, 0.1f, 100.0, 200f, 500);
   ```

2. Если диаметр ≠ 50 мкм → Z работает, проблема в расчете
3. Если диаметр ≈ 50 мкм → Z НЕ работает, проблема в SDK или сканере

**ВАЖНО:** Алгоритм расчета ПРАВИЛЬНЫЙ (точный порт Java), проблема в применении Z координаты сканером!
