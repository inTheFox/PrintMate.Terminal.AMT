# Итоговые изменения: Порт Java алгоритма диаметра пучка

## 📋 Что было сделано

### 1. ✅ Полностью переписан TestUdmBuilder.cs
**Было:** Использовалась линейная интерполяция диаметра и экспериментальные измерения из `BeamDiameterCalibration`

**Стало:** Точный C# порт Java методов из:
- `hans-dev/Hans4Java/src/org/iiv/hlssystem/multi/UdmProducer.java`
- `hans-dev/Hans4Java/src/org/iiv/hans4java/controlCardProfiles/BeamConfig.java`

**Ключевые изменения:**
- ❌ Убрана линейная интерполяция
- ❌ Убран BeamDiameterCalibration
- ✅ Добавлено управление состоянием (как в Java: `curBeamDiameterMicron`, `curPower`)
- ✅ Точный порт методов:
  - `GetCorrectZValue()` ← Java line 103-117
  - `GetCurrentFocalLengthMm()` ← Java line 119-121
  - `GetLensTravelMicron()` ← Java line 199-202
  - `GetCalculationZValue()` ← Java line 205-209

### 2. ✅ Создан UdmBuilderJavaPort.cs
Полнофункциональный UDM builder для использования в продакшене:
- Работает со слоями и регионами
- Поддерживает полилинии
- Точно повторяет логику Java UdmProducer

**Расположение:** `PrintMate.Terminal/Hans/UdmBuilderJavaPort.cs`

### 3. ✅ Создан набор тестов
**Новые файлы:**
- `DiameterVerificationTest.cs` - Автоматические тесты с различными диаметрами и мощностями
- `RunDiameterTests.cs` - Простой интерфейс для запуска
- `scanator_config_test.json` - Рабочая конфигурация
- `DIAMETER_TEST_README.md` - Подробная документация

## 🔑 Ключевая разница: НЕТ интерполяции

### Старый алгоритм (неправильный)
```csharp
// ❌ НЕПРАВИЛЬНО: Вычислял Z offset один раз для региона
float zOffset = CalculateZOffset(beamDiameter);

// Затем применял этот offset ко всем точкам
for (point in points) {
    point.z = zOffset;  // Одинаковая Z для всех точек
}
```

### Новый алгоритм (Java порт)
```csharp
// ✅ ПРАВИЛЬНО: Устанавливает текущий диаметр (как DIAMETER operation)
_currentBeamDiameterMicron = beamDiameter;

// Затем для каждой точки вычисляет Z с учетом (x, y)
for (point in points) {
    // Учитывает координаты точки!
    double focalLength = sqrt(x² + y² + baseFocal²);
    double lensTravelMicron = rayleighLength * sqrt((d/d₀)² - 1);
    focalLength += lensTravelMicron;

    // Преобразует через полином
    point.z = a*f² + b*f + c;
}
```

## 📊 Алгоритм расчета (пошагово)

### Шаг 1: Устанавливаем текущее состояние
```csharp
// Эквивалент DIAMETER operation (UdmProducer.java line 242-246)
_currentBeamDiameterMicron = beamDiameter;

// Эквивалент POWER operation (UdmProducer.java line 231-240)
_currentPowerWatts = power;
```

### Шаг 2: Для каждой точки вычисляем Z
```csharp
float GetCorrectZValue(x, y, z):
    // 2.1. Базовое фокусное расстояние
    //      (BeamConfig.java line 119-121)
    focalLengthMm = sqrt(x² + y² + (baseFocal + z)²)
    focalLengthMicron = focalLengthMm * 1000

    // 2.2. Добавляем смещение от диаметра (если включено)
    //      (BeamConfig.java line 105-107, 199-202)
    if (EnableDiameterChange):
        lensTravelMicron = zR * sqrt((d/d₀)² - 1)
        focalLengthMicron += lensTravelMicron

    // 2.3. Вычитаем power offset (если включен и power > 15% max)
    //      (BeamConfig.java line 108-110, 188-197)
    if (EnablePowerOffset && power > maxPower * 0.15):
        powerOffset = GetPowerOffset(power)
        focalLengthMicron -= powerOffset

    // 2.4. Преобразуем через полином
    //      (BeamConfig.java line 111-116, 205-209)
    if (EnableZCorrection):
        f = focalLengthMicron / 1000  // мкм -> мм
        Z = a*f² + b*f + c
    else:
        Z = z  // Для SLM всегда 0
```

## 🚀 Как использовать

### Для тестирования (HansDebuggerApp)
```csharp
using HansDebuggerApp.Hans;

// Быстрый тест
RunDiameterTests.RunQuick(diameter: 100.0, power: 200f);

// Полный набор тестов
RunDiameterTests.RunAll();
```

### Для продакшена (PrintMate.Terminal)
```csharp
using PrintMate.Terminal.Hans;

var builder = new UdmBuilderJavaPort(config);
string binFile = builder.BuildLayer(layer);
```

## 📝 Конфигурация

Ваш рабочий конфиг сохранен в `scanator_config_test.json`:

```json
{
  "beamConfig": {
    "minBeamDiameterMicron": 63.1,      // d₀ - минимальный диаметр (фокус)
    "rayleighLengthMicron": 1863.0,      // zR - длина Рэлея
    "focalLengthMm": 538.46              // Фокусное расстояние линзы
  },
  "thirdAxisConfig": {
    "afactor": 0.0,                      // a = 0 (линейный полином!)
    "bfactor": 0.013879796,              // b
    "cfactor": -7.4747047                // c
  },
  "functionSwitcherConfig": {
    "enableDiameterChange": true,        // ✓ Включено
    "enableZCorrection": true,           // ✓ Включено
    "enablePowerOffset": true,           // ✓ Включено
    "enablePowerCorrection": true        // ✓ Включено
  }
}
```

## 🎯 Результаты

### Точность расчетов
При правильной конфигурации ожидается:
- ✓ Отлично: ошибка < 1 мкм
- ✓ Хорошо: ошибка < 3 мкм
- ⚠ Приемлемо: ошибка < 10 мкм

### Проверка на реальном оборудовании
1. Запустить тесты: `RunDiameterTests.RunAll()`
2. Найти UDM файлы в `UdmBinFiles/`
3. Загрузить файл в Hans сканер
4. Прожечь точки
5. Измерить реальный диаметр пятен
6. Сравнить с заданным диаметром

## ⚠️ Важные замечания

1. **Линейный полином** - В вашей конфигурации `a=0`, значит полином линейный: `Z = b*f + c`

2. **Power offset** - Включен, но работает только при `power > 75 Вт` (15% от 500 Вт)

3. **Расфокусировка** - При `d > 94.65 мкм` (1.5*63.1) начинается предупреждение о потере интенсивности

4. **Координаты (x, y)** - Влияют на расчет! Чем дальше от центра, тем больше фокусное расстояние

5. **Диапазон Z** - Рекомендуется держать в пределах [-0.5, 0.5] мм

## 📚 Дополнительные материалы

### Файлы для изучения
1. `TestUdmBuilder.cs` - Тестовый builder с подробными комментариями на русском
2. `UdmBuilderJavaPort.cs` - Продакшен builder с комментариями-ссылками на Java код
3. `DIAMETER_TEST_README.md` - Полная документация по тестам

### Java исходники
- `hans-dev/Hans4Java/src/org/iiv/hlssystem/multi/UdmProducer.java`
- `hans-dev/Hans4Java/src/org/iiv/hans4java/controlCardProfiles/BeamConfig.java`
- `hans-dev/Hans4Java/Test/org/iiv/hans4java/controlCardProfiles/BeamConfigTest.java`

## ✅ Чек-лист проверки

- [x] Удалена интерполяция диаметра
- [x] Портированы все методы из BeamConfig.java
- [x] Портирована логика UdmProducer.java
- [x] Создан набор автоматических тестов
- [x] Добавлена подробная документация на русском
- [x] Сохранен рабочий конфиг
- [ ] **TODO: Протестировано на реальном оборудовании**
- [ ] **TODO: Измерены реальные диаметры пятен**
- [ ] **TODO: Сравнены с расчетными значениями**

## 🔬 Следующие шаги

1. **Запустить тесты:**
   ```csharp
   RunDiameterTests.RunAll();
   ```

2. **Проверить UDM файлы:**
   - Найти в папке `UdmBinFiles/`
   - Проверить что файлы создаются без ошибок

3. **Тестирование на оборудовании:**
   - Загрузить файлы в Hans сканер
   - Прожечь тестовые точки
   - Измерить реальные диаметры
   - Заполнить таблицу результатов:

   | Заданный диаметр (мкм) | Z координата (мм) | Измеренный диаметр (мкм) | Ошибка (%) |
   |------------------------|-------------------|--------------------------|------------|
   | 63.1                   |                   |                          |            |
   | 70.0                   |                   |                          |            |
   | 80.0                   |                   |                          |            |
   | 100.0                  |                   |                          |            |
   | 120.0                  |                   |                          |            |
   | 150.0                  |                   |                          |            |

4. **При необходимости скорректировать конфиг:**
   - Если ошибка > 10%, проверить параметры BeamConfig
   - Пересчитать длину Рэлея
   - Проверить коэффициенты полинома Z-коррекции

---

**Дата создания:** 2025-01-20
**Версия:** 1.0
**Статус:** Готов к тестированию на оборудовании
