<UserControl x:Class="HansDebuggerApp.Views.PLCLogicIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:componentsViews="clr-namespace:HansDebuggerApp.Views"
             mc:Ignorable="d" 
             >
    <Border Background="White" Padding="10" CornerRadius="5" Margin="1" HorizontalAlignment="Stretch">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Grid.Column="0" FontSize="15" x:Name="TextBlock" TextAlignment="Left" HorizontalAlignment="Left" TextWrapping="Wrap" Foreground="Black" Text="{Binding CommandInfo.Title, RelativeSource={RelativeSource AncestorType={x:Type componentsViews:PLCLogicIndicator}}}"/>
            <Image Width="20" x:Name="Image" Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>
